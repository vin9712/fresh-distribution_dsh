-- =============================================================================
-- s17：送货单打印包装包（P2/D-050）——【已随 D-055 退役，本脚本不再执行】
--
-- 原内容：t_print_package 新建 + t_print_task 重建（一次打印动作建包、包内任务清单、
--         队列连续输出、逐张回执）。
--
-- 退役原因（D-055，2026-09-03）：送货单不再是独立单证，「按批次建包连续打印 N 张点单」
--   的前提（先生成送货单）消失；打印改为 客户+日期(+配送点) 实时取数单张输出。
--   后端 DeliveryPrintPackage*/DeliveryPrintTask* 与前端 printPackageDrawer.vue 已整体删除。
--
-- 现状：远端库仍残留 t_print_package / t_print_task（含 2026-09-02 真机验证的测试数据）
--       与改名备份 t_print_task_bak_20260902。清理动作见 s23_dead_print_tables_retire.sql
--       （默认注释保护，需人工确认无历史依赖后执行）。
--
-- 本文件保留为占位说明，避免后人按编号找不到脚本；新环境重建（init_all.sql）不创建这两张表。
-- =============================================================================

SELECT 's17 已随 D-055 退役，无需执行（清理见 s23）' AS note;
