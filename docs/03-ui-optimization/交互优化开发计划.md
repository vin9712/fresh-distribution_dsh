# 生鲜配送 ERP 交互优化 — 开发计划（分阶段执行）

> 版本：v1.0（待评审）
> 日期：2026-08-22
> 依据文档：
> - `生鲜配送 ERP 交互优化方案.md`（交互优化方案，P0~P4 优先级）
> - `../assets/报价demo.csv`（本期商品价格 demo，用于初始化商品库/商品信息）
> - `../02-redesign-r1/r1_redesign_progress.md`（前序重构进度，当前基线）
> 目标前端：`RuoYi-Vue3`（Vue3 + Element Plus + vxe-table@4 + Pinia 3），后端 `lin-distribution`（端口 8090），前端 dev 端口 1025。

---

## 0. 背景与目标

系统已完成基础信息模块重构（R1，SKU 客户无关化 + 客户商品池 + 配送点覆盖 + 默认模板），
但 `t_product_sku` 已重建为空表（遗留问题 #1：**需业务侧重新录入标准 SKU**），且交互方案 P0~P4
的体验断点（草稿丢失、无常用商品、流程无闭环、报价录入低效等）尚未落地。

本开发计划分阶段执行：

| 阶段 | 内容 | 对应方案 | 状态 |
|------|------|---------|------|
| **Phase 0** | 用 `报价demo.csv` 初始化商品库(SPU)+商品信息(SKU)+报价 | —（数据地基） | ✅ 完成（2026-08-22） |
| **Phase 1** | 销售订单详情页：草稿自动保存 / 常用商品面板 / 下拉别名高亮 | 方案 §3.1（P0） | ✅ 完成（2026-08-22） |
| Phase 2 | 销售订单列表页：生成采购单/送货单抽屉 | 方案 §3.2（P1） | ✅ 完成（2026-08-23） |
| Phase 3 | 全局搜索 Ctrl+K | 方案 §3.4（P2） | ✅ 完成（2026-08-23） |
| Phase 4 | 报价详情页：Excel 式批量调价 / 异常预警 / 固定列 （毛利率按需延后） | 方案 §3.3（P3） | ✅ 完成（2026-08-23） |
| Phase 5 | 验收单移动模式 + 权限/字典一致性修复 | 方案 §3.5 / §3.6（P4） | ✅ 完成（2026-08-24；代码+回归+SQL+e2e 冒烟 20 项全过） |

每阶段独立提交、独立验证，按顺序推进，一个阶段验收通过后再进入下一阶段。

---

## Phase 0：用报价demo.csv 初始化商品库与商品信息（本轮执行）

### 目标

将 `../assets/报价demo.csv`（88 条商品，7 个商品类型）灌入开发库：

1. **商品分类**：补齐 CSV 中用到的分类（幂等，不破坏现有分类树）
2. **商品库 SPU**：以 CSV **品名**（剥离括号/斜杠规格信息后的基础名称）建 SPU（`t_product_spu`）
3. **商品信息 SKU**：每行 CSV 建 1 个标准 SKU（`t_product_sku`，客户无关）：
   `name` = 品名，**括号/斜杠内的规格信息 → `spec_name`**（如 `新鲜土豆(大)` → name=新鲜土豆 / spec_name=大），
   `sale_price` = 本期执行价
4. **报价单**：为演示客户「丽宫」(id=10) 生成一张**已发布**报价单（88 条明细，本期执行价，
   `product_name`=品名、`product_spec`=规格快照），使录单取价（配送点覆盖 > 客户报价 > 模板）立即可用
5. **编号序列衔接**：`biz_code_seq.sku_code` 与 `skuQuote:{当日}` 调整，避免与系统自动生成冲突

### 品名 / 规格拆分规则（Q3 已确认）

- 正则剥离行尾的 `(...)`、`（...）`、`/...` 后缀作为 `spec_name`，剩余为基础品名
- 涉及行（已核实）：`新鲜土豆(大)`、`新鲜土豆/100g以上`、`青尖椒(大)`、`青尖椒(中)`、`铁棍山药（蔬菜）`
  - 例：`新鲜土豆(大)` → SPU「新鲜土豆」+ SKU(name=新鲜土豆, spec_name=大)
  - 例：`新鲜土豆/100g以上` → SKU(name=新鲜土豆, spec_name=100g以上)（同品名不同规格，SKU 唯一键 `(category_id,name,spec_name,unit)` 天然区分）
- 无后缀行：`spec_name` 留空
- 助记码基于**品名**生成（与后端 `PinYinConvertUtils` 规则一致）

### CSV 数据事实（已核实）

- 88 条数据行，无重复名称、无重复（类型,名称,单位）组合
- 编码 UTF-8 with BOM、CRLF 换行、部分价格带尾随空格（如 `3.50 `，需 trim）
- 分类分布：叶菜类 19 / 根茎类 31 / 瓜类 10 / 菌菇类 8 / 葱蒜类 6 / 半成品类 7 / 佐料类 7
- 单位：87 条「斤」、1 条「袋」（娃娃菜）

### 分类映射（待用户确认，见文末问题 Q1）

| CSV 商品类型 | 现有分类 | 处理（Q1 已确认） |
|--------------|---------|------------------|
| 叶菜类 | id=50（蔬菜类下） | 复用 |
| 根茎类 | id=51（蔬菜类下） | 复用 |
| 菌菇类 | id=54（蔬菜类下） | 复用 |
| 佐料类 | id=56（蔬菜类下） | 复用 |
| 瓜类 | id=52「瓜果类」 | **复用瓜果类** |
| 葱蒜类 | 不存在 | **新建**二级分类（蔬菜类下，code 110009） |
| 半成品类 | 不存在 | **新建**二级分类（蔬菜类下，code 110010） |

### 实现方式

**生成器 + SQL 存档 + 验证脚本**（复用现有 `.dsh-e2e/` 工具链）：

1. 新增 `.dsh-e2e/gen-product-init.js`（Node）：
   - 读 `../assets/报价demo.csv`（utf-8-sig 去 BOM，trim 价格）
   - 用 `pinyin-pro`（复用 `RuoYi-Vue3/node_modules/pinyin-pro`）预计算每行助记码
     （与后端 `PinYinConvertUtils.toFirstChar` 同规则：名称拼音首字母大写）
   - 生成 `../../sql/s7_product_init.sql`（幂等写法，见下）
2. 新增 `../../sql/s7_product_init.sql`（生成产物，提交存档）：
   - 分类：`INSERT ... ON DUPLICATE KEY UPDATE`（name 全局唯一，天然幂等）
   - SPU：唯一键 `(category_id, name)` → `INSERT IGNORE` + `SELECT` 回填 id；
     name = 品名（剥离规格后缀）；多条 SKU 对应同一品名时仅建 1 条 SPU
   - SKU：唯一键 `(category_id, name, spec_name, unit)` → `INSERT IGNORE`；
     name = 品名、spec_name = 规格后缀（无则空）；
     `code` 显式 `S00000001`~`S00000088`（不与现有 S00000001/2 冲突，当前库仅 2 个 SKU）
   - 报价单头+明细：按 code `BJ{yyyymmdd}xxxxx` 先查重，存在则跳过（幂等）；
     状态 status=1 发布、valid=1、生效今天 ~ +30 天；
     明细 `product_name`=品名、`product_spec`=规格、`price`=本期执行价
   - 序列衔接：`UPDATE biz_code_seq SET seq=88 WHERE biz_key='sku_code'`（当前 seq=2）；
     `skuQuote:{当日}` 设为当日已生成报价单数（防重号）
3. 用 `.dsh-e2e/apply-sql.js sql/s7_product_init.sql` 应用
4. 新增 `.dsh-e2e/verify-product-init.js`（Node + mysql2，仿 `verify-s0-*.js`）：
   - SKU 总数 = 88（新增）+ 2（原有）= 90；SPU 总数 = 88
   - 抽检：大白菜/娃娃菜(袋)/新鲜土豆(大) 的 category/unit/sale_price/mnemonic
   - 报价单 1 张、明细 88 条、价格=本期执行价、status=1、valid=1
   - 幂等：脚本重跑一次，数量不翻倍

### 关键文件

| 文件 | 动作 |
|------|------|
| `.dsh-e2e/gen-product-init.js` | 新增（CSV→SQL 生成器） |
| `../../sql/s7_product_init.sql` | 新增（生成产物，提交存档） |
| `.dsh-e2e/verify-product-init.js` | 新增（校验脚本） |
| `交互优化开发计划.md` | 本次新增 |

### 复用

- `.dsh-e2e/apply-sql.js`：既有 SQL 应用脚本（mysql2，multipleStatements）
- `.dsh-e2e/verify-*.js`：既有校验脚本模式（mysql2 查询断言）
- `RuoYi-Vue3/node_modules/pinyin-pro`：助记码预计算
- 现有分类：`t_product_category` 表 id 50/51/52/54/56（蔬菜类子类）
- `biz_code_seq`：`sku_code`（S+8 位）、`skuQuote:{yyyyMMdd}`（BJ 单号）序列机制

---

## Phase 0 执行记录（✅ 2026-08-22 完成）

### 产出文件

| 文件 | 说明 |
|------|------|
| `.dsh-e2e/gen-product-init.js` | CSV→SQL 生成器（pinyin-pro 预计算助记码；品名/规格拆分）※本地脚本，`.dsh-e2e/` 已被 gitignore（与既有 verify-* 惯例一致） |
| `../../sql/s7_product_init.sql` | 生成产物（幂等，可重复执行，已提交） |
| `.dsh-e2e/verify-product-init.js` | 验证脚本（35 项断言 + `--reapply` 幂等重放校验）※本地脚本，未入库 |

### 执行结果

- **数据**：分类 2 个新建（葱蒜类 110009 / 半成品类 110010）；SPU 86（85 新 + 复用既有大白菜）；
  SKU 88（编码 S00000003~S00000090，含规格拆分：新鲜土豆 大/100g以上、青尖椒 大/中、铁棍山药 蔬菜）；
  报价单 BJ2026082200001（丽宫，已发布，2026-08-22~09-21，88 明细）；客户商品池 88 条（C10000003~C10000090）
- **序列衔接**：`sku_code→90`、`skuQuote:20260822→1`、`customer_sku_code:10→90`
- **API 冒烟**（后端 8090）：SKU 分页 total=90、SPU 分页 total=86、`/product/quote/active/10` 返回 BJ2026082200001、中文搜索命中（需 UTF-8 编码；Git Bash curl 默认按 GBK 发参是测试环境问题，浏览器不受影响）
- **幂等**：`--reapply` 重放后 35 项断言全部通过，数量不翻倍

### 过程中发现并修复的 Bug（取价引擎）

- **现象**：`/price/query?customerId=10&skuId=3` 返回 `price:null`，已发布报价不生效
- **根因**：`ProductSkuQuoteDetailMapper.xml` 的 `selectActivePriceByCustomerAndSku` 条件为 `sq.valid = 0`
  （2026-08-17 commit 3e1db22 引入），与 `selectCustomerActiveQuote`（valid=1 & status=1）及
  发布逻辑（生效报价 valid=1）语义矛盾，导致**客户报价层从未真正命中过取价**；单测 mock mapper 故未暴露
- **修复**：`sq.valid = 0` → `sq.valid = 1`（仅此一处误用，已全仓排查）
- **验证**：`mvn -pl lin-distribution clean install -o -DskipTests` + 44 单测全绿；重启后
  `/price/query` 命中：大白菜 1.20 / 娃娃菜 2.40 / 新鲜土豆(大) 1.60，均 source=2
- **回归守护**：verify-product-init.js 增加「取价 SQL 使用 valid=1」与「取价等价 SQL 断言」两项检查

### 说明

- 客户商品池（customers_sku）88 条为计划外补充：录单商品源 = 客户商品池 + 临时商品，
  报价 UI 明细也从客户商品池组装，不加池则演示客户无法在录单/报价中看到这批商品
- 既有测试数据（西红柿/试吃草莓 SKU、大白菜 SPU）保留不动，新编码从 S00000003 起避开唯一键

---

## Phase 1 执行记录（✅ 2026-08-22 完成）

### 1.1 草稿自动保存

- 新增 `RuoYi-Vue3/src/utils/saleDraft.js`：localStorage 草稿工具（key `saleDraft:order:{orderId}` / `saleDraft:new:{customerDeptId}`，24h 过期，读取时自动清理）
  - **实现说明**：项目实际使用 Vuex（`src/store`），Pinia 在依赖中但未接线；为避免引入第二套状态库，改为轻量工具模块（职责等价，无全局状态依赖）
- `order/sale/detail.vue`：
  - 头部状态标签：`草稿保存中...`（灰）→ `已于 HH:mm:ss 自动保存`（绿）；无实质内容时 `未保存`（橙）
  - 触发：`orderForm`/`orderDetailList` 深度 watch + **5s 防抖**（取代方案的 30s 兜底，数据变更必触发）+ `beforeunload` 立即保存（刷新/关页不丢）
  - 脏判定 `isOrderDirty`：明细对比（复用 checkTableUpdted）+ 表单字段快照对比，加载/重置后不产生空草稿
  - 恢复：新单页顶部常驻横幅「检测到未完成的订单草稿【配送点】（保存于 HH:mm）点击恢复/忽略」；恢复校验配送点仍存在（树已加载时），恢复后删除草稿；保存成功后清除对应草稿
- `order/sale/index.vue`：列表页顶部横幅「检测到 N 个草稿... 去恢复/全部忽略」，去恢复携带 `?draft=key` 跳转，详情页自动恢复

### 1.2 常用商品面板

- 后端新增 `GET /order/saleDetail/frequent?customerId=&days=&limit=`（权限 `order:saleDetail:list`）
  - `SaleOrderDetailMapper.selectFrequentSkuList`：近 N 天（默认30，封顶90）**已确认及后续状态**订单按 sku_id 聚合下单次数，Top N（默认20，封顶50）；临时商品(sku_id 空)不参与
  - `SaleOrderDetail` 增加瞬时字段 `orderCount`/`lastOrderTime`
  - 新增 `SaleOrderDetailServiceImplTest`（4 用例：空客户不查库 / 默认参数 / 自定义透传 / 封顶）
- 前端右侧 8 栏改 `el-tabs`：`常用`（近30天下单频率 Top，点击卡片→追加明细行→三层取价→光标落数量列，末尾自动补空行）/ `最近`（原最近订单表单+列表原样保留）

### 1.3 商品搜索下拉别名高亮

- `vxe-pulldown` 商品名称列改为命名插槽渲染：匹配关键词包 `<span class="kw-hl">` 标红高亮（先转义再高亮，防 XSS）；别名/临时来源追加 `el-tag`：`[别名]`（warning）/`[临时]`（info）
- 数据映射补充 `sourceTag`：客户商品池命中客户别名（alias ≠ skuName）→ 别名；临时商品 → 临时

### 验证结果

- 后端：`mvn -pl lin-distribution test -o` 48 用例全绿（44+4）；curl：frequent 接口返回西红柿/大白菜（e2e 历史订单），UTF-8 中文参数需百分号编码（Git Bash curl 按 GBK 发参为测试环境现象，浏览器不受影响）
- 前端：`npm run build:prod` 0 报错；puppeteer 冒烟 `.dsh-e2e/verify-phase1.js` 12 项全过：Tabs / 级联选配送点 / 常用商品加载与点击插入 / 草稿 5s 防抖落盘 / 状态标签 / 刷新横幅 / 恢复 / [别名]标签 / 关键词高亮
- 列表页横幅验证：注入草稿→列表页显示横幅→去恢复跳转详情自动恢复
- 全链路回归 `.dsh-e2e/e2e-full-flow.js` 通过（录单→确认→采购→送货→打印→送达→验收→销售日报）

### 过程中修复的存量问题

- `deepCloneWithoutFields` 的 `structuredClone` 对 Vue3 响应式 Proxy 抛 `DataCloneError`（`JSON.parse(JSON.stringify)` 兜底）：该问题在重置/关闭/切换订单时同样会触发，属存量隐患，本次由草稿脏判定路径暴露后修复
- 冒烟中发现并确认 `keyupProductNameEvent` 依赖 vxe-input 的 `input`+`keyup` 事件链（与真实输入一致）；puppeteer 合成按键不触发 vxe-input 内部状态更新，属测试手段差异，非功能缺陷（已用等价手动派发验证）

---

## Phase 2：销售订单列表页生成采购单/送货单抽屉（方案 §3.2，P1）

- 实现 `RuoYi-Vue3/src/views/order/sale/index.vue` 中 `handleBuildPurchase` / `handleBuildDelivery`
  - 勾选已确认订单 → 点击按钮 → 右侧 `el-drawer`（三步：汇总预览按品类分组 → 填写信息（采购：供货商/采购员；送货：配送日期）→ 确认生成）
  - 生成走既有接口：`PurchaseOrderController`（含明细组装）、`DeliveryOrderController`
  - 生成后列表行显示小图标（购物车/货车），悬浮显示生成的采购单号/送货单号
- 复用：`api/order/purchase.js` / `api/order/delivery.js` 既有创建接口；确认 `PurchaseOrderServiceImpl`/`DeliveryOrderServiceImpl` 是否已有「按订单生成」能力（无则在本阶段补服务方法+单测）
- 验证：puppeteer 全流程（勾选→抽屉三步→生成→图标单号出现）；`mvn -pl lin-distribution test -o` 全绿

## Phase 2 执行记录（✅ 2026-08-23 完成）

### 2.1 后端：新增「按勾选订单生成」能力（此前仅按配送日期生成）

- **新增接口**：
  - `POST /order/sale/generatePreview`（权限 `order:sale:list`）：抽屉第一步汇总预览——校验订单均为已确认，按品类分组聚合明细（join `t_product_sku`/`t_product_category`，临时商品归「临时商品」），返回订单头信息 + 分组明细 + 总数量/总金额
  - `POST /purchase/generate-by-orders`（权限 `purchase:add`）：按勾选订单生成采购单，body `{orderIds, supplierName, purchaser}`；校验订单存在且已确认、配送日期一致；按 sku_id 合并数量；`source_order_ids` 记录来源订单
  - `POST /order/delivery/generate-by-orders`（权限 `order:delivery:add`）：按勾选订单生成送货单，body `{orderIds, deliveryDate?}`（配送日期可调整，默认订单配送日期）；按 客户+配送点 分组、明细带 `order_id`/`order_code`
- **幂等守卫**：
  - 采购：扫描既有自动采购单 `source_order_ids` 与所选订单求交，任一命中即拒绝（兼容按日期生成的旧单）
  - 送货：`t_delivery_order_detail.order_id` 唯一键 + 前置 `selectListByOrderIdIn` 校验
- **新增 DTO**：`PurchaseByOrdersDTO` / `DeliveryByOrdersDTO` / `SaleOrderGeneratePreviewDTO` / `SaleGeneratePreviewVO`（含 PreviewOrder/PreviewGroup/PreviewItem）
- **Mapper 新增**：`SaleOrderDetailMapper.selectAggregatedByOrderIds`（送货生成口径，含 order_id/order_code）、`selectPreviewByOrderIds`（预览口径，含 categoryName）、`PurchaseItemMapper.selectSummaryByOrderIds`（采购汇总口径）
- **列表联动字段**：`SaleOrderMapper.selectSaleOrderVo` 增加两个子查询——`purchaseOrderCode`（`JSON_CONTAINS(source_order_ids, ...)` 命中自动采购单）、`deliveryOrderCode`（`t_delivery_order_detail.order_id` 命中送货单）；`SaleOrder` 增加瞬时字段
- **采购员字段**：`purchase_order` 新增 `purchaser` 列（`../../sql/s8_purchase_delivery_drawer.sql`，information_schema 探测幂等）；domain/XML/采购单列表页同步

### 2.2 前端（`RuoYi-Vue3/src/views/order/sale/index.vue`）

- `handleBuildPurchase` / `handleBuildDelivery` 落空实现 → 右侧 `el-drawer`（600px，非阻塞可随时关闭）三步：
  1. **汇总预览**：已选订单 tag + 按品类分组的商品表（分类头 + 明细行 + 合计），底部总行数/总金额
  2. **填写信息**：采购→供货商/采购员（可空）；送货→配送日期（默认订单配送日期，可调整）
  3. **确认生成**：el-descriptions 汇总（类型/订单数/商品行数/总金额/供货商/采购员或配送日期）+ 防重复提示
- 生成成功：`生成成功：PC...` / `生成成功：HS...、HS...` 提示，抽屉关闭，列表刷新
- **行图标**：新增「流程单据」列——有采购单显示购物车图标（橙）、有送货单显示货车图标（绿），`el-tooltip` 悬浮显示单号
- 前置校验：未勾选提示；含非审核状态订单提示「请选择审核状态的订单」且不开抽屉
- API：`sale.js` 加 `generatePreview`；`purchase.js` 加 `generatePurchaseByOrders`；`delivery.js` 加 `generateDeliveryByOrders`；采购列表页加「采购员」列

### 2.3 验证结果

- **后端**：`mvn -pl lin-distribution test -o` 60 用例全绿（48→60，新增 `PurchaseOrderServiceImplTest` 7 用例 + `DeliveryOrderServiceImplTest` 5 用例）
- **API 冒烟**（`.dsh-e2e/verify-phase2-api.js`，14 项全过）：预览分组（叶菜类/根茎类）、采购单生成+供应商/采购员落库、明细数、重复生成拒绝（采购+送货）、送货单按 客户+配送点 分组且明细带 order_id/order_code、列表 `purchaseOrderCode`/`deliveryOrderCode` 回填、未确认订单预览被拒
- **前端**：`npm run build:prod` 0 报错；puppeteer 冒烟 `.dsh-e2e/verify-phase2.js` 27 项全过：过滤→勾选已确认订单→抽屉三步→生成成功→行图标→悬浮单号（采购+送货）→含草稿订单被拦截且不开抽屉
- **全链路回归** `.dsh-e2e/e2e-full-flow.js` 通过（录单→确认→采购→送货→打印→送达→验收→日报，不受影响）

### 过程中发现并修复的 Bug

- **`checkOrdersNotInAutoPurchase` 幂等失效**：`throw new ServiceException(...)` 写在 `try` 内，被兜底 `catch (Exception ignored)`（原意是跳过非 JSON 历史数据）吞掉，重复生成采购单不报错 → 重构为 try 只包 JSON 解析、异常 continue，抛错移到循环体

### 关键文件

| 文件 | 动作 |
|------|------|
| `lin-distribution/.../dto/PurchaseByOrdersDTO.java` 等 4 个 DTO | 新增 |
| `lin-distribution/.../service/impl/PurchaseOrderServiceImpl.java` / `DeliveryOrderServiceImpl.java` / `SaleOrderServiceImpl.java` | 新增 generateByOrderIds / generatePreview |
| `lin-distribution/.../controller/PurchaseOrderController.java` / `DeliveryOrderController.java` / `SaleOrderController.java` | 新增 3 个接口 |
| `lin-distribution/.../mapper/*.xml`（SaleOrderDetail/PurchaseItem/SaleOrder/PurchaseOrder） | 新增聚合/预览 SQL + purchaser 字段 + 列表联动子查询 |
| `lin-distribution/.../domain/SaleOrder.java` / `SaleOrderDetail.java` / `PurchaseOrder.java` | 新增瞬时字段 / purchaser |
| `../../sql/s8_purchase_delivery_drawer.sql` | 新增（purchase_order.purchaser 幂等 ALTER） |
| `lin-distribution/src/test/.../PurchaseOrderServiceImplTest.java` | 新增（7 用例） |
| `lin-distribution/src/test/.../DeliveryOrderServiceImplTest.java` | +5 用例 |
| `RuoYi-Vue3/src/views/order/sale/index.vue` | 抽屉三步 + 流程单据图标列 |
| `RuoYi-Vue3/src/views/purchase/index.vue` | +采购员列 |
| `.dsh-e2e/verify-phase2-api.js` / `verify-phase2.js` | 新增验证脚本（本地） |

---

## Phase 3：全局搜索 Ctrl+K（方案 §3.4，P2）

- 新增组件 `RuoYi-Vue3/src/components/GlobalSearch/index.vue`：
  - 全屏居中 `el-dialog`，仿 VS Code Ctrl+P
  - 分组结果：客户（名称+别名）/ 商品（名称+编码+分类）/ 订单（单号+客户+日期）
  - 键盘 ↑↓ 选择、回车跳转（`window.open` 新标签打开对应详情）
  - 监听 `window.keydown`，过滤 `compositionstart/end`（中文输入不误触）
- 消费 QuickTable 已预留的 `global-search` 事件：在 `layout`（`RuoYi-Vue3/src/layout/index.vue`）挂监听，任一面板 Ctrl+K 均可唤起
- 录单页有草稿时跳转前先保存（联动 Phase 1 的 `saleDraft` store）
- 验证：puppeteer 冒烟（输入关键词→分组出现→回车跳转）；build:prod 0 报错

## Phase 3 执行记录（✅ 2026-08-23 完成）

### 3.1 后端：全局搜索 `/search/global`（客户 / 商品 / 订单 三组命中）

- **接口**：`GET /search/global`（权限为空，登录即可，供全局浮层检索）——`keyword` 必填、`limit` 缺省 10 封顶 30
  - `GlobalSearchServiceImpl.search`：keyword trim 后为空则返回空分组（不查库）；limit ≤0 视为缺省、>30 封顶 30
- **命中规则**（`GlobalSearchMapper.xml`）：
  - 客户：`t_customer` is_deleted=0，匹配 name / alias / tel / address
  - 商品：`t_product_sku` is_deleted=0，匹配 name / code / mnemonic_code / `product_alias.alias`（子查询，含客户别名）
  - 订单：`t_sale_order` is_deleted=0，匹配 code / 客户 name / 客户 alias
- **新增文件**：`GlobalSearchController.java` / `GlobalSearchVO.java`（含 CustomerHit/ProductHit/OrderHit 内部类）/ `GlobalSearchMapper.java` / `GlobalSearchService.java` / `GlobalSearchServiceImpl.java` / `GlobalSearchMapper.xml`
- **单测**：`GlobalSearchServiceImplTest`（5 用例：空关键词不查库 / 空白关键词 / 缺省 10 / 超限封顶 30 / 三组透传）

### 3.2 前端：`GlobalSearch` 组件 + 全局唤起

- **新增** `RuoYi-Vue3/src/components/GlobalSearch/index.vue`（全屏居中 `el-dialog`，仿 VS Code Ctrl+P）：
  - 分组结果：客户（名称+别名+电话）/ 商品（名称+编码+分类+规格）/ 订单（单号+客户+配送点+日期）；`gs-type-tag` 类型标签、关键词红色高亮（先转义防 XSS）、命中数角标
  - 键盘：↑↓ 跨分组连续移动（按 uid 扁平化）、↵ 新标签打开、Esc 关闭；`compositionstart/end` 过滤中文输入误触
  - 跳转：`window.open(url, '_blank')` —— 客户→`/basicInfo/customer-dept/index/{id}`、商品→`/basicInfo/sku?name={name}`（SKU 页新增 `?name=` 预填，`sku/index.vue` created 回填 queryParams.name）、订单→`/order/sale-detail/index?orderId={id}`
  - 草稿联动：跳转前 `window.dispatchEvent('sale-draft-flush')`，`order/sale/detail.vue` 监听该事件并复用 `handleBeforeUnload` 立即落盘（不触发浏览器关闭确认，安全）
  - 组件挂载于 `layout/index.vue`（`<GlobalSearch />`），内部 `window.keydown` 全局监听（快捷键面板 Ctrl+K 任意页面均可唤起，不依赖 QuickTable 布局）
- **新增 API** `RuoYi-Vue3/src/api/search.js`：`globalSearch(keyword, limit)`

### 3.3 验证结果

- **后端**：`mvn -pl lin-distribution test -o` 65 用例全绿（60→65，新增 `GlobalSearchServiceImplTest` 5 用例）
- **API 冒烟**（curl UTF-8 编码）：客户「丽宫」命中、商品「大白菜/新鲜土豆」命中、订单 `XD202608170001` 命中；中文由 Git Bash GBK 终端发送需百分号编码（测试环境现象，浏览器不受影响）
- **前端**：`npm run build:prod` 0 报错（仅有 Dart Sass 弃用警告，存量问题）；dev（1025）编译通过
- **puppeteer 冒烟** `.dsh-e2e/verify-phase3.js`（gitignore 本地脚本）：17 项全过——Ctrl+K 唤起 / 商品分组+大白菜 / 客户分组+丽宫 / 键盘 ↓ 切换激活项 / 激活项类型标签 / 客户→配送点详情新标签 / 商品→SKU 页新标签 / 订单→订单详情新标签 / Esc 关闭

### 3.4 冒烟脚本修复（测试侧 Bug，非应用缺陷）

1. **跳转误判**：持久化 Edge Profile 残留历史标签（`about:blank`/旧页），`browser.pages().find(p => p !== page)` 匹配到残留页导致误报「about:blank」。修复：测试开头关闭全部残留标签 + 用 `browser.waitForTarget` 精确捕获本次 `window.open` 新建标签（puppeteer-core@8 无 `page.waitForEvent`）
2. **Esc 关闭误判**：Element Plus `el-dialog` 关闭后内层 `.global-search-dialog` 仍 `display:block`（保留 DOM），但父级 `.el-overlay` 变 `display:none`。修复：改为检测 `.el-overlay` 的 `display` 判定关闭

### 关键文件

| 文件 | 动作 |
|------|------|
| `lin-distribution/.../controller/GlobalSearchController.java` | 新增（/search/global）|
| `lin-distribution/.../dto/GlobalSearchVO.java` | 新增（三组 Hit 内部类）|
| `lin-distribution/.../mapper/GlobalSearchMapper.java` / `GlobalSearchMapper.xml` | 新增（三组命中 SQL）|
| `lin-distribution/.../service/GlobalSearchService.java` / `impl/GlobalSearchServiceImpl.java` | 新增 |
| `lin-distribution/src/test/.../GlobalSearchServiceImplTest.java` | 新增（5 用例）|
| `RuoYi-Vue3/src/components/GlobalSearch/index.vue` | 新增 |
| `RuoYi-Vue3/src/api/search.js` | 新增 |
| `RuoYi-Vue3/src/layout/index.vue` | 挂载 <GlobalSearch /> |
| `RuoYi-Vue3/src/views/order/sale/detail.vue` | 监听 sale-draft-flush 落盘 |
| `RuoYi-Vue3/src/views/product/sku/index.vue` | 支持 ?name= 预填 |
| `.dsh-e2e/verify-phase3.js` | 新增（本地，gitignore）|

---

## Phase 4：报价详情页 Excel 式批量调价（方案 §3.3，P3）

文件：`RuoYi-Vue3/src/views/product/quote/detail.vue`

1. **键盘流**：`keyboard-config` 定制——Enter 仅在「新报价」列向下移动，其余列 Tab 移动
   （vxe `edit-config` + `activated`/`keydown` 事件处理）
2. **批量填充**：右键菜单「向下填充 / 向上填充」（`menu-config` v4 结构 `{ body: { options: [[...]] } }`，图标用 `vxe-icon-custom-column`）；顶部工具栏「批量调价」按钮：输入涨幅%（如 5），对选中行统一计算新报价
3. **固定列**：商品名称列 `fixed: 'left'`、操作列 `fixed: 'right'`
4. **异常预警**：新报价偏离上次价/成本价 >30% 或 < -10% → 单元格橙色背景/边框（`cell-class-name`），提示「涨幅异常」
5. **毛利率实时显示**：`毛利率% = (新报价 - 成本价) / 新报价`，<15% 标红
   - **待确认**：成本价数据源（见文末 Q5：`t_product_sku` 现无 cost 字段，可选加字段 / 用参考售价近似 / 报价模板取价）

验证：build:prod 0 报错；puppeteer 冒烟（右键填充、批量调价、异常高亮）

---

## Phase 4 执行记录（✅ 2026-08-23 完成）

### 产出文件

| 文件 | 动作 |
|------|------|
| `RuoYi-Vue3/src/views/product/quote/detail.vue` | 重写报价详情表格区：Excel 式批量调价 / 异常预警 / 固定列 / 上次价 |
| `.dsh-e2e/verify-phase4.js` | 新增验证脚本（21 项断言，本地，gitignore） |

### 决策记录（Q5）

- **Q5（成本价数据源）**：本轮**不做**。用户明确「毛利率可以不用实现」，故不新增 `t_product_sku.cost_price` 字段；
  异常预警的基准采用客户商品池返回的 **SKU 参考售价 `sku_sale_price`**（作为「上次价」，展示于 上次价 列），无需后端改动。

### 实现要点

1. **列结构**：新增复选框列（供批量调价勾选）+ 上次价 + 新报价 + 操作列；`商品名称` fixed left、`操作` fixed right、
   复选框/序号 fixed left；`上次价` 只读、`新报价` 可编辑（仅此列过 `beforeEditMethod`）。
2. **键盘流**：沿用 vxe-table 默认 `isEnter`（在新报价列提交并向下移动）+ `isTab`（横向）——已验证连续录入：
   点击某行新报价 → 输入 → Enter 提交并下移 → 直接键入下一行即连续录入。
3. **批量填充**：右键单元格弹出「向下填充 / 向上填充」（`menu-config` v4 结构 `{ body: { options: [[...]] } }`，trigger=cell）；
   向下填充复制当前值到其下所有行、向上填充到其上所有行。
4. **批量调价**：顶部工具栏「批量调价」→ 输入涨幅% → 对勾选行按 **当前新报价**（为空则按 上次价）×(1+pct%) 计算新报价；
   未勾选则提示。
5. **异常预警**：新报价相对 上次价 涨幅>30% 或 跌幅>10% → 单元格橙色背景/边框（`cell-class-name`）+ 价格旁 `!` 图标 + 顶部计数标签；
   **仅当报价>0 才判定**（避免客户商品池中未报价商品 price=0 的误报）。
6. **删除行**：操作列「删除」按钮可移除当前报价行（提交后生效）。

### 验证结果

- `npm run build:prod` 0 报错（仅存量 Dart Sass 弃用警告）
- `.dsh-e2e/verify-phase4.js` 21 项全过：无页面错误 / 列结构含 上次价+新报价+操作 / 上次价已填充 /
  基准无异常 / 编辑触发异常标黄+计数 / 批量调价弹窗+确认(+50%→3.00) / 右键向下+向上填充 / 键盘 Enter 连续录入 / 删除行 / 窄视口左右固定列包装

---

## Phase 5：验收移动模式 + 权限/字典一致性（方案 §3.5/§3.6，P4）

1. **验收单移动模式**（`RuoYi-Vue3/src/views/order/acceptance/index.vue`）：
   - 验收弹窗右上角「移动模式」切换按钮
   - 移动模式：仅显示商品名称+实收数量两大输入框，每屏 2~3 条，扫码枪连续扫码定位并累加实收数量；仅改布局，不改保存逻辑
2. **3.6.1 配送点页权限**：`sys_menu` 补 `partner:customerDept:*` 权限项（`../../sql/` 新脚本）；同时前端按钮兜底（可选移除 `v-hasPermi`）
3. **3.6.2 客户类型字典**：客户列表列加兜底渲染（无值显示 `-`）+ 补 `t_customer_type` 字典数据（SQL）
4. **3.6.3 路由权限拼写**：`RuoYi-Vue3/src/router/index.js` 中报价详情 `permissions: ['partner:quote:add']` → `['product:quote:add']`
5. **3.6.4 旧权限残留**：清理 `price:point:*` 相关菜单/权限 SQL（已被 `price:delivery-override:*` 取代）

验证：puppeteer 冒烟（非 admin 角色可见配送点按钮、报价详情路由可注册、字典有值）；SQL 脚本幂等可重跑

## Phase 5 执行记录（✅ 2026-08-24 全部完成）

### 5.1 出品文件

| 文件 | 动作 |
|------|------|
| `../../sql/s9_phase5_perms_dict.sql` | 新增：配送点权限 `partner:customerDept:*`（6 个 `sys_menu`，menu_id 2100~2105）+ `t_customer_type` 字典 4 条 + 清理 `price:point:*`；全部幂等（INSERT IGNORE / WHERE NOT EXISTS / DELETE） |
| `RuoYi-Vue3/src/views/order/acceptance/index.vue` | 验收明细对话框头部「移动模式」切换按钮 + 移动模式（扫码栏 / 每屏 3 条卡片 / 分页 / 扫码定位累加实收），仅改布局不改保存逻辑 |
| `RuoYi-Vue3/src/views/partner/customer/dept.vue` | 移除 新增/修改/删除/导出 按钮的 `v-hasPermi`（前端兜底，按钮始终可见） |
| `RuoYi-Vue3/src/views/partner/customer/index.vue` | 客户类型列无值兜底渲染 `-`（`dict-tag` v-if 判空） |
| `RuoYi-Vue3/src/router/index.js` | 报价详情路由 `permissions: ['partner:quote:add']` → `['product:quote:add']` |
| `lin-distribution/.../domain/AcceptanceItem.java` | 新增瞬时字段 `skuCode`（非表字段） |
| `lin-distribution/.../mapper/AcceptanceItemMapper.xml` | `selectAcceptanceItemVo` 改为 `left join t_product_sku sku on ai.sku_id=sku.id` 取 `sku.code as sku_code` |
| `../../sql/init_all.sql` / `../../sql/s2_2_price_menu.sql` | 菜单「配送点报价」改名「配送点覆盖」，perms 由 `price:point:*` 改为 `price:delivery-override:*` |
| `.dsh-e2e/verify-phase5.js` | 新增冒烟脚本（本地，gitignore）：移动模式扫码/分页 / 字典兜底 / 报价路由可注册 / 配送点按钮可见 |

### 5.2 验证结果（已确认）

- **后端单测**：`mvn -pl lin-distribution test -o` → **65 用例全绿，BUILD SUCCESS**（AcceptanceItem skuCode 改动无回归）
- **前端构建**：`cd RuoYi-Vue3 && npm run build:prod` → **0 报错**（无 `Iphone`/`ArrowLeft` 图标缺失）
- **SQL 已应用**（查询确认）：`t_customer_type` 字典 4 条（餐馆/食堂/商超/商户）、`partner:customerDept:*` 六个菜单存在、`price:point:*` 无残留、`price:delivery-override:*` 存在
- **后端 API**：`GET /acceptance/1/items` 正确返回 `skuCode`（`S00000010/04/05/06/07`），顺序与 verify 脚本预期一致
- **数据核对**：验收单 `YS20260817001(id=1)` 草稿 status=0 且 5 条明细（大白菜/椰菜/奶白菜/菜芯/小白菜）；`S00000005`=奶白菜、`S00000006`=菜芯，与移动模式扫码用例匹配
- **交互式浏览器冒烟**（`.dsh-e2e/verify-phase5.js`）：**2026-08-24 新 session 重跑 ✅ 全部通过（20/20）**：移动模式扫码定位+累加+分页切换+退出 / 客户类型字典兜底 / 报价详情路由可注册 / 配送点页按钮可见。修复 verify 脚本 3 个误判（`type` NOT NULL 用 `''` 替代 `NULL`；实收数量 `4` vs `4.00` 格式改用 Number 比较）

### 5.3 环境注意事项（重要）

- 本次会话 shell 在 **Windows bash / PowerShell / WSL** 间不稳定切换，且前后端 dev 服务中途被停；MySQL(3306)/Redis(6379) 正常。
- **Maven 必须用 `mvn.cmd`**：WSL 下 `mvn` bash 脚本使用 Unix classpath（`:`），而 Windows JVM 需要 `;`，会报 `org.codehaus.plexus.classworlds.launcher.Launcher` 找不到。已确认 Windows `JAVA_HOME=D:\Java\jdk17\azul-17.0.13`。
- 已生成本地辅助批处理（未入 git，可复用）：`deploy/run_mvn_test.bat`（跑单测）、`deploy/run_npm_build.bat`（跑前端构建）、`deploy/start_be.bat` / `deploy/start_fe.bat`（拉起后端/前端服务）。
- **新 session 重跑 e2e 步骤**：
  1. 起服务：`deploy/start_be.bat`（后端，等端口 8090 就绪，约 120s）+ `deploy/start_fe.bat`（前端 1025）
  2. `cd .dsh-e2e && node verify-phase5.js`（Phase 5 冒烟）— ✅ 2026-08-24 已通过
  3. 全链路回归：`node e2e-full-flow.js`（可选）— ✅ 已确认无影响
  4. `git add -A && git commit` — ✅ 2026-08-24 已提交（c66def1）

---

## 通用技术要点（执行各阶段时注意）

- vxe-table v4 `menu-config` 结构为 `{ body: { options: [[...]] } }`，非 v3 直数组
- 右键菜单图标：`vxe-icon-wipe` 不存在，用 `vxe-icon-custom-column`
- 树形表格不支持 `checkbox-config.range`（QuickTable 已自动降级）
- 列设置持久化需 grid 唯一 `id` + 每列 `field`
- 全局快捷键需过滤 `compositionstart/end`（中文输入法）
- 大数据表格开启虚拟滚动（`scroll-y.gt=100`）
- 后端改动后启动前必须 `mvn -pl lin-distribution clean install -o -DskipTests`（否则旧 XML 残留）

---

## 验证总览（每个 Phase 验收）

| 项 | 命令/脚本 |
|----|----------|
| 后端单测 | `mvn -pl lin-distribution test -o`（当前基线 65 用例全绿） |
| 前端构建 | `cd RuoYi-Vue3 && npm run build:prod`（0 报错） |
| 数据初始化 | `.dsh-e2e/verify-product-init.js`（Phase 0） |
| E2E 冒烟 | `.dsh-e2e/verify-*.js`（puppeteer + Edge，登录 admin/admin123） |
| 全链路回归 | `.dsh-e2e/e2e-full-flow.js`（录单→确认→采购→送货→打印→送达→验收→日报） |

---

## 决策记录（2026-08-22 已确认）

- **Q1 分类映射** ✅：已有分类直接复用；瓜类 → 复用「瓜果类」(id=52)；葱蒜类、半成品类新建二级分类（蔬菜类下，编码 110009/110010）
- **Q2 本期执行价** ✅：写入 SKU 参考售价 `sale_price`，并为「丽宫」(id=10) 生成一张已发布报价单（88 明细、今天起 30 天）
- **Q3 SPU 粒度** ✅：建立 SPU，但**只筛选品名**（剥离括号/斜杠规格信息），规格信息放入 SKU 的 `spec_name`
- **Q4 本轮范围** ✅：先执行 Phase 0（数据初始化）+ 验证，验收通过后依次推进 Phase 1~5
- **Q5（Phase 4 时再确认）**：报价毛利率的「成本价」数据源——新增 `t_product_sku.cost_price` 字段 / 用参考售价近似 / 从报价模板取
  - **已定（2026-08-23）**：毛利率本轮不实现（用户确认）；异常预警基准 = SKU 参考售价 `sku_sale_price`（上次价），未新增 cost 字段
