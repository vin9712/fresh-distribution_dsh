# 订单-送货-验收链路：开发目标与进度总控

> **状态**：✅ 全部完成（设计 v1.2 定稿 2026-08-28；T1～T8 已全部完成，2026-08-28 文档收口）
> **用途**：本文是唯一进度看板。开发按 §五 切片推进，每完成一项勾选并更新 §六。
> **文档关系**：业务推演见《[销售订单与送货单关系设计](../销售订单与送货单关系设计.md)》（D-001~D-037），落地方案见《[订单-送货-验收链路详细设计](订单-送货-验收链路详细设计.md)》（库表/服务/接口/页面明细）。

---

## 一、目标一句话

把「订单→送货→验收」从**按客户+日期推断关联**的现状，改造为**来源显式建模**的三层模型：
**配送批次（客户日总表，采购配货用）→ 外部送货单（客户签收/验收凭证）→ 打印渲染**；
验收实收**落到配送点粒度、归验收单所有**；订单行只留生成时冻结的应送分配台账。

## 二、已确认决策（浓缩版，全部 2026-08-28 定稿）

| # | 决策 |
|---|---|
| 三层模型 | `t_delivery_batch`(客户+日期唯一) → `t_delivery_order`(客户签收单，scope 由客户策略决定) → 打印层；内部总表不落物理明细，由台账聚合 |
| 组单策略 | 客户级配置 `doc_scope_type`：CUSTOMER_DATE(跨点总单,delivery_point_id=NULL) / DELIVERY_POINT_DATE(每点一张)；只按客户配置，生成瞬间快照进批次与单据 |
| 聚合规则 | merge_same_item=true 时按五元组合并：`sku+品名快照+单位+规格+单价`；**不同价必拆行**（D-024）；merge=false 一订单行一行 |
| 来源台账 | `t_delivery_source_item`：订单行→送货行分配，生成时冻结，含唯一键(近期一订单行只送一次)；仅 4 个结构性用途：状态回写/幂等/作废释放/撤回判断。**无实收数据** |
| 验收 | 独立验收单为唯一实收录入点（C1 定稿）；明细按**配送点×商品**展开（A类总单）、文员直接在各点行填实收（录入即归属）；**订单行级不做实收派生**（基线 D-026"到订单行"作废）；双向差异均必填原因（1短收/2超收两套字典） |
| 结算 | 依据=验收单 total_amount；`t_sale_order_detail.actual_*` 降级只读镜像列（验收提交时同步），禁止业务直写 |
| 回写 | 送达/验收/撤回全部按 `source_item` 的 DISTINCT sale_order_id 回写，废除 `updateStatusByDeliveryGroup` 按客户+日期推断 |
| 撤回/改单 | 撤回判断=EXISTS 有效 source_item 分配（作废即解锁）；订单编辑护栏：DELIVERED/ACCEPTED 拒改、CONFIRMED+已分配拒改、SETTLED 拒改 |
| 作废/补单 | 送货单 PENDING/PRINTED 可作废(原因必填,新号)；未打印补单=作废重建(整客户覆盖,D-022)；已打印补单=补充单 doc_kind=1(D-023)；手工补生成按客户+日期补齐全部遗漏(D-025) |
| 生成入口 | **手工为主**（录单页「本客户订单已录完」按钮 > 订单列表勾选/按客户 > 定时兜底）；定时 23:30(次日)+06:00 兜底双窗口幂等；总表=工人采购配货来源之一 |
| 撤销验收 | 验收页 SUBMITTED→DRAFT，原因必填+完整审计快照；任一来源订单 SETTLED 则拒绝 |
| 退货 | 验收后真实退货走独立退货单 TH（不撤回历史验收）；换货=退货单+新销售订单；数量上限=实收-累计已退，单价锁原验收价；质检结果仅记结论（reusable/damaged），库存流水待库存模块 |
| 退役 | OrderAdjustment 写入口隐藏→接口标废→下版清理；订单页快速验收三接口(`/actual/*`)与实收编辑列下线 |

## 三、数据库变更（= `sql/s14_delivery_acceptance_redesign.sql` 全部内容）

| 对象 | 动作 |
|---|---|
| `t_delivery_batch` | 新建（客户+日期唯一，策略快照 scope_type/merge/template/layout_json） |
| `t_delivery_source_item` | 新建（delivery_id/detail_id/order_id/order_detail_id/dept/分配量/价，unique(order_detail_id)+is_deleted） |
| `t_acceptance_revoke_log` | 新建（snapshot_json/reason/by/time） |
| `t_return_order` + `t_return_item` | 新建（TH 编号；item 含 quality_result；settle_scope 期间冲销快照） |
| `t_job_run_log` | 新建（定时任务告警数据源，工作台消费） |
| `t_delivery_order` | 加 batch_id/scope_type/doc_kind(0正常1补充)/predecessor_id/void_reason/void_by/void_time |
| `t_delivery_order_detail` | **DROP INDEX unq_order_id**（order_id 列留作历史兼容） |
| `t_acceptance_item` | `loss_quantity→difference_quantity` 更名；加 customer_dept_id、reason_type(1短收2超收) |
| `t_acceptance` | 加 revoke_reason/revoked_by/revoked_time |
| `t_customer` | 加 doc_scope_type/doc_merge_same_item |
| 字典 | delivery_no_print_reason / acceptance_shortfall_reason / acceptance_overage_reason / return_quality_result |
| 编号/菜单 | biz_code_seq 加 returnOrder→TH；退货单菜单权限、送货"作废/补生成"按钮权限、验收"撤销"权限、验收单菜单恢复 |
| ~~diff_alloc~~ | v1.0 曾计划的差异分摊表**已废除**（点级录入即归属，无需此表） |

## 四、代码改造总览

| 层 | 内容 |
|---|---|
| 枚举 | `DeliveryOrderStatus` 加 VOIDED(3) |
| 新服务 | `DeliveryGenerationService`：generateForDate(定时)/generateForCustomer(手工/补单) 统一入口；三态分支（无单/全待打印→作废重建/含已打印→补充单）；事务整体回滚 |
| 改造 | `AcceptanceServiceImpl`：createByDeliveryOrder(A类按点展开)、updateDraft(双向原因)、submit/revoke(IN 回写+审计)；`SaleOrderServiceImpl`：编辑护栏+撤回精确化；`markDelivered(id, noPrint?)` 免纸原因 |
| 删除 | `generateByDeliveryDate/generateByOrderIds`、`selectAggregatedByDeliveryDate/ByOrderIds`（聚合移 Java 侧）、`updateStatusByDeliveryGroup`、`countByCustomerIdAndDeliveryDate` |
| 新接口 | generate/customer/{id}/{date}、delivery/{id}/void、delivered(带noPrint)、{id}/sources、/delivery/batch/view、group-preview、acceptance/{id}/revoke、**acceptance/by-order/{orderId}(去验收定位)**、/order/return CRUD+inspect、workbench/job-alert |
| 下线 | `/order/detail/actual/draft|accept|revoke` + DTO/Service 实现 |
| 页面 | 订单列表(送货单号列/可撤回标识/已配送行"去验收"按钮)、detail.vue(移除实收编辑)、送货单页(作废/送达原因弹窗/来源视图/补充单tag)、新批次总表页、验收页(点级分组+来源对照列+highlightOrder定位+撤销)、客户表单(策略两字段)、新退货单页、workbench(待验收指回+生成异常告警) |

## 五、实施切片与进度

> 顺序执行；每片完成标准 = 单测全绿 + E2E 更新通过 + `build:prod` 0 error。

- [x] **T1 地基**（2026-08-28 完成，commit 见 §六）
  - [x] s14 SQL（§三 全部）+ init_all.sql 合并（幂等重跑已在本地 MySQL 8 验证通过，见 §六 2026-08-28 债务②）
  - [x] 枚举 VOIDED / 实体 / Mapper resultMap / 字典 SQL
  - [x] 存量回填脚本说明（§5 注释模板：仅近期单据可精确回填，合并生成行标"历史无来源"）
- [x] **T2 正确性修复**（G2/G3/G4/G6）（2026-08-28 完成，commit 见 §六）
  - [x] existsValidAllocation + 订单编辑护栏 + 撤回精确化（新增 SaleOrderServiceImplTest ×10）
  - [x] markDelivered/submit 改 IN 回写（updateStatusByDeliveryGroup 废除）
  - [x] E2E 造“未进单同组订单”验证不被误更新（页面级 E2E 待 T7 前端切片后在真实流程补验；本轮已在远程库事务+ROLLBACK 内完成 SQL 级验证，见 §六）
- [x] **T3 统一生成服务**（2026-08-28 完成验证收口，commit 见 §六）
  - [x] DeliveryGenerationService + 三态分支 + 幂等单测（17 例全绿）
  - [x] ScheduledTasks 23:30/06:00 双窗口 + job_run_log（代码已落，随本片验证）
  - [x] generate 接口委托（旧入口兼容，含新增 customer/{id}/{date} 端点，随本片验证）
- [x] **T4 作废与补单**（2026-08-28 完成，commit 见 §六）
  - [x] voidDeliveryOrder + 作废重建/补充单流程 + 操作日志（作废重建/补充单三态分支 T3 已落，本片补手工作废入口与字典/免纸送达，验收见 §六）
- [x] **T5 验收口径**（2026-08-28 完成，commit 见 §六）
  - [x] 双向原因/reason_type + A类点级展开 + revoke + 审计
  - [x] actual_* 镜像同步 + 订单页接口下线
- [x] **T6 退货单**（2026-08-28 完成，commit 见 §六）
  - [x] CRUD + 数量上限/单价锁 + inspect + settle_scope
- [ ] **T7 前端**
  - [x] 第一轮：T2-T4 配套界面（2026-08-28 完成，含后端配套：by-order 定位 / sources 来源视图 / 订单列表送货单号修复+可撤回标识，见 §六）
  - [x] 第二轮：T5/T6 界面 + OrderAdjustment 入口隐藏 + workbench 告警 + 「本客户订单已录完」按钮 + 批次总表页 + 验收页 highlightOrder 定位（2026-08-28 完成，见 §六）
- [x] **T8 文档收口**（2026-08-28 完成，commit 见 §六）
  - [x] 基线文档 D-026 修订标注（头部 v1.0r1 修订说明 + D-026 行 + §6.2/§6.3 修订注 + §12 指向详细设计与进度文，方案A 交互设计标注已下线）
  - [x] DESIGN.md §7.1/7.2/7.3 修订标注并指向新文档（§0 文档体系登记详细设计为专项权威；§7.2 补 VOIDED/补单/免纸/来源回写；§7.3 补客户日总表配货来源；详细设计文档状态转已定稿）
  - [x] manuals 重写送货/验收章节（操作手册新增 v1.3 迭代标签、重写 §3.2/3.4/3.5、新增 §3.7 客户日总表/§3.8 退货单、第六章/FAQ/编码规则/术语表同步；文员指南重写 ③ 验收为验收单页流程、修正页面地图/工作台/出单按钮/FAQ/取价优先级遗留错误）

## 六、进度日志（追加式）

| 日期 | 事项 |
|---|---|
| 2026-08-28 | 设计 v1.2 全定稿（C1/C2/D1~D5 关闭）；本文建立；T1 未开工 |
| 2026-08-28 | **T1 完成**：`sql/s14_delivery_acceptance_redesign.sql`（新表×6 / 幂等 ALTER×5 / 字典×5 / 菜单 portable 写法 / 回填注释模板）+ init_all.sql 追加 [20] 节 + Java 地基（DeliveryOrderStatus.VOIDED；新实体 DeliveryBatch/DeliverySourceItem/AcceptanceRevokeLog/ReturnOrder/ReturnItem/JobRunLog + Mapper×6；DeliveryOrder/Acceptance/AcceptanceItem/Customer 实体与 XML 扩展；ReportMapper.xml 改用 difference_quantity 列，Java 字段 lossQuantity 保留）；`mvn -pl lin-distribution -am compile` 通过 |
| 2026-08-28 | ✅ 债务① 已解决：s7~s12 增量已合并入 init_all.sql（后因 r1 并入顺延为 [15]~[21] 节） |
| 2026-08-28 | ✅ 债务② 已解决：s14 幂等重跑已在本地 MySQL 8 验证——空库全量重建 0 错误 → s14 连续重跑×2 均 0 错误且快照零变化 → 非空数据下再跑 0 错误且既有数据不被触碰 |
| 2026-08-28 | 验证过程中顺带修复 init_all.sql 三处历史问题：① [10]/[12]/[18]/[23] 节及源文件硬编码库名 `fresh-distribution-dsh`（共 70 处，导致在非同名库重建时语句打到真实库）→ 全部去除；② r1 基础信息重构（r1_basicinfo_redesign + r1_frontend_menu）从未合并 → 并入为 [08]/[09] 节（[15] 节 s7 依赖其 t_product_sku.spec_name 结构）；③ [27] 节 s5_2 固定 dict_id=109 与 [21] 节 s12 自增字典冲突 → 改自增（字典按 dict_type 字符串引用，id 无业务含义）；init_all.sql 现共 29 节 |
| 2026-08-28 | ✅ 债务④ 已解决：`mvn -pl lin-distribution -am compile` 使用 JDK 17 编译通过（0 error），编译命令已确认 |
| 2026-08-28 | ✅ 债务③ 已闭环：核实现库仅 admin（超管放行）与 common（仅框架菜单，无业务菜单，只有演示账号 ry）两角色，**无实际受影响对象**；新增 `sql/s14b_role_grant_template.sql` 幂等授权模板（按 role_key 参数化，含 UI 勾选清单，已在测试库验证：执行×2 绑定不重复、角色不存在时空跑安全）；未来建业务角色后改 @role_key 执行或按清单勾选即可，生效需重新登录 |
| 2026-08-28 | ✅ 远程库（39.96.76.240）补跑 s14 完成：执行前全量备份 `sql/db_bak/before_s14_20260828_005602.sql`（--single-transaction，已验完整）；执行×2 幂等验证通过；6 新表 / 13 新列 / 4 组新字典 / 菜单 2106~2115 全部就位，既有数据零损伤；s14b 模板跳过（无受影响角色）。⚠️ 远端旧应用此后调用验收/日报接口（loss_quantity 已改名）会报错，属预期窗口期，待新版部署 |
| 2026-08-28 | **T2 完成**（G2/G3/G4/G6）：① G2/G3——`updateStatusByDeliveryGroup` 删除，`markDelivered`/`submit` 改 `updateStatusByDeliveryId`（按 source_item DISTINCT 订单 IN 回写，同客户同日未进单订单不再被误更新）；② G4——撤回判断改 `existsValidAllocation`（EXISTS 有效分配，作废即解锁），“同客户同日有送货单即拒绝”旧逻辑废除；③ G6——`checkOrderEditable` 护栏（DELIVERED/ACCEPTED 拒改→引导新增销售订单/退货单；SETTLED 拒改；CONFIRMED 已分配拒改→先作废送货单；DRAFT/CONFIRMED 未分配放行，替代原 update 路径 "order status must be NEW" 硬限制），另补订单不存在空判断；④ `countByCustomerIdAndDeliveryDate` 删除。测试：新增 `SaleOrderServiceImplTest`×10（护栏各状态+撤回三场景），`DeliveryOrderServiceImplTest`/`AcceptanceServiceImplTest` 改断言 IN 回写+重复操作 never()；lin-distribution pom 固定 surefire 3.2.5（原默认 2.12.4 不识别 JUnit5，CLI 此前从未真正跑过测试）；`mvn -pl lin-distribution test` **70 全绿**（曾报 72，多出的 2 例系 DeliverySkuOverride 模块删除后 target 内陈旧 class 残留被首次可用的 surefire 捞起，clean 后核实真实数为 70，模块早已在 a2ad126 整体移除）、全工程 compile 0 error。SQL 语义在远程库事务+ROLLBACK 实测：IN 回写只命中进单方、existsValidAllocation 判定与作废释放解锁均符合设计，测试数据零残留（T2 无前端改动，build:prod 不适用，随 T7 补） |
| 2026-08-28 | **T3 完成验证收口**：① 修复预期编译错误——DeliveryGenerationServiceImpl 写 source_item 时 createTime 传 Date 应为 LocalDateTime，改用 nowLdt（LocalDateTime.ofInstant 转换，同事务内时间源一致）；另补测试类缺失的 DeliveryGenerationService 接口 import；② 首跑 17 例中 5 失败，两类根因：**(a) 合并行归属维度口径**（251/291 两例）——实现原"仅跨订单/跨点合并才置空"与设计不符，按《详细设计》§5.1 步骤5 修正：A类总单明细行 customer_dept_id 一律置空（dept 维度仅留 source_item），合并产生的多来源行 order_code 一律置空（订单归属维度留 source_item 对照列，G1 口径；未合并单来源行保留）；**(b) generateForDate 测试 stub 缺口**（490/528/546 三例）——实现为并发幂等护栏在逐客户事务内会重查 O_missed（批行 FOR UPDATE 锁后重读，防 23:30/06:00/手工并发重复建单，设计保留），测试只 stub 了日期级查询导致重查返回 null 走幂等跳过，补齐逐客户 stub ×3；③ 终态：`mvn -pl lin-distribution test` **78 全绿**（61 旧 + 17 新，旧 DeliveryOrderServiceImplTest 保留打印/送达 4 例），全工程 compile 0 error；application-local.yml 本地环境配置改动不入库，db_bak 备份沿用 T2 惯例不入库；T3 无前端改动，build:prod 不适用随 T7 补 |
| 2026-08-28 | **T4 完成**（作废与补单）：① 新增手工作废入口 `voidDeliveryOrder(id, reasonCode, reasonNote)`（DeliveryOrderService/Impl）：准入 PENDING/PRINTED（DELIVERED/VOIDED 拒绝，已送达单走验收撤销）；已有 SUBMITTED 验收拒绝（AcceptanceMapper 按 deliveryOrderId+status 查询）；任一来源订单 SETTLED 拒绝（source_item→selectSaleOrderByIdIn，拒绝信息带订单号）；动作=置 VOIDED+原因/人/时间+source_item 软删释放（G4 解除锁定，旧行保留审计）；原因落库格式=「字典标签[：补充说明]」（DictUtils 解析，缓存不可用回退原编码；other 必填说明）；操作日志由 Controller `@Log(title="送货单作废")` 写 sys_oper_log（含请求参数）。② 免纸送达改造（D-018）：`markDelivered(id, DeliveryNoPrintDTO)` 重载，PENDING 直送必须选免纸原因（字典 delivery_no_print_reason，other 必填说明），拼接 `[免纸送达:标签[：说明]]` 落 remark；PRINTED 送达无需登记（误传也不落备注）；旧单参入口 `markDelivered(id)` 保留兼容（内部委托 null→PENDING 被拒）；Controller 新增 `PUT /{id}/delivered`（body 可选），旧 `PUT /{id}/deliver` 标 @Deprecated 保留。③ SQL：新增 `sql/s14c_delivery_void_reason.sql` 作废原因字典（录单错误/重复生成/客户取消/缺货取消/其他）+ init_all.sql 追加 [30] 节；权限复用 s14 已建的 order:delivery:void。④ 测试：DeliveryOrderServiceImplTest 4→17 例（作废成功 PENDING/PRINTED、重复作废/已送达/不存在/参数校验/SUBMITTED 验收/SETTLED 拒绝、免纸送达 4 例、已作废不可送达），`mvn -pl lin-distribution test` **91 全绿**（78 旧基线 + 13 新）；全工程 compile 0 error。远程库：执行前全量备份 before_s14c_20260828_085419.sql，s14c 执行×2 幂等验证通过（5 条字典数据零重复）；作废重建/补充单流程的 doc_kind/predecessor 正确性由 T3 单测覆盖（三态分支），T4 验收要点达成；T4 无前端改动，build:prod 不适用随 T7 补 |
| 2026-08-28 | **T5 代码落盘（先行条目，验证收口待办）**：① `AcceptanceServiceImpl` 重写三处 + 新增 revoke——(a) `createByDeliveryOrder` 拆 `expandByPoint`/`expandByDetail`：A类总单按「配送点×商品」展开（§3.2③ 录入即归属），聚合键=(customer_dept_id, delivery_detail_id)——生成侧五元组合并保证同一 detail 即同一五元组，A类跨点合并行内各点来源按 dept 拆开即可，规格/单位/价格快照从送货明细行取（DeliverySourceItem 无 spec/unit 列，勿再改 schema）；B/C类=送货明细行展开并补记 dept（detail.customerDeptId 空回退 delivery_point_id 兼容历史单）；A类无 source_item（历史单）回退 expandByDetail 并 warn；金额/差异/排序统一在返回前重算；(b) `updateDraft` 双向原因（D-013/G8）：diff<0→reasonType=1 短收/必填（提示「短收差异必须填写原因：品名」），diff>0→reasonType=2 超收/必填（提示「超收差异…」），diff=0→reason/reasonType 置 null 清空；⚠️ 配套 AcceptanceItemMapper.xml `updateAcceptanceItem` 的 reason_type/loss_reason 改为无条件写（原 `<if>` 判空无法清空，仅 updateDraft 一处调用，已确认无其他调用方）；(c) `submit` 增 `syncActualMirror`：按「点级实收×应送占比」把实收镜像同步到订单行 actual_*（ratio=actual/delivered 6位舍入，行实收=allocated×ratio 2位舍入，单价锁 source_item.unit_price 快照，经 SaleOrderDetailMapper.updateActualBatch 单行写入，loss_reason 无条件写 null 即清空）；历史单无 source_item 跳过（镜像列允许空，结算以验收单 total_amount 为准）；(d) 新增 `revoke(id, reason)`（§5.4/Q16/D-014）：准入 SUBMITTED；任一来源订单 SETTLED 拒绝（source_item→selectSaleOrderByIdIn，提示带订单号）；动作=快照 JSON（fastjson2，acceptance+items）落 t_acceptance_revoke_log → 验收单回 DRAFT+记 revoke_reason/revoked_by/revoked_time → 来源订单 ACCEPTED→DELIVERED（IN 回写）→ clearActualByOrderId 清镜像；操作人 SecurityUtils try/catch 回落 system；② 接口/DTO：新增 `AcceptanceRevokeDTO`（reason @NotBlank）+ `POST /acceptance/{id}/revoke`（perm 复用 s14 已建 `acceptance:revoke`，@Log 验收单撤销）；③ 订单页接口下线（C1 定稿）：删除 SaleOrderDetailController `/actual/draft|accept|revoke` 三端点、SaleOrderDetailService/Impl 三方法（saveActualDraft/confirmAcceptance/revokeAcceptance）、SaleOrderActualDraftDTO/SaleOrderAcceptDTO 两 DTO（全工程无残留引用）；updateActualBatch/clearActualByOrderId 保留（镜像同步/撤销复用）；④ 无 SQL 变更：customer_dept_id/reason_type/revoke 列与 t_acceptance_revoke_log 均 s14 已建，远程库无需动作；⑤ 主代码 `mvn -pl lin-distribution -am compile` 0 error，**未提交**。**⚠️ T5 续作清单（下次 session 直接从这里继续）**：⑥ 更新旧 AcceptanceServiceImplTest（现 9 例中 3 例会被 T5 口径打破）：`录入时实收金额按后端单价重算`（超送 1 需补 lossReason，否则被新双向必填拦）；`损耗为负必须填写原因`（断言消息改含「短收差异必须填写原因」）；`提交后状态变更且来源订单进入已验收`（submit 现会调 acceptanceItemMapper.selectListByAcceptanceId + deliverySourceItemMapper.selectListByDeliveryId 做镜像同步，需 stub 空列表或造数验证）；⑦ 新增测试：A类点级展开（跨点合并行拆出点级行、delivered=SUM(allocated)、dept/sku/规格/单价快照正确、历史单回退）、B/C类 dept 回填、双向原因四例（短收/超收/缺原因拒绝/无差异清空）、revoke 四例（成功：审计快照+回草稿+ACCEPTED→DELIVERED+清镜像 / SETTLED 拒绝 / 非SUBMITTED 拒绝 / 原因必填）、镜像同步占比分摊；⑧ `mvn -pl lin-distribution test` 全绿 + 全工程 compile 后勾选 §五 并 git commit（勿提交 application-local.yml 与 sql/db_bak/）；⑨ 遗留提示：`GET /acceptance/by-order/{orderId}`（去验收定位）设计归 T5 邻接但进度文 §五 未列，暂随 T7 前端第一轮一起做 |
| 2026-08-28 | **T3 代码落盘（先行条目，验证收口见上）**：① 新增统一生成服务 `DeliveryGenerationService/Impl` + `DeliveryGenerateTrigger`/`DeliveryScopeType` 常量 + `GenerateResultVO`——O_missed 双路判定（source_item 有效分配 + 历史单 detail.order_id 兼容，防并存期双重配送）；三态分支（V 空→正常生成 / V 全 PENDING→作废重建 D-022：原单置 VOIDED+source_item 软删释放+重建覆盖全部已确认订单 / V 含已打印→补充单 doc_kind=1 D-023）；批次 UPSERT FOR UPDATE + 已存在复用快照（D-016）；聚合移 Java 侧：按 scope_type 分组（A 类 delivery_point_id=NULL、dept 仅留 source_item）、五元组合并（价格去尾零比较）不同价必拆行（D-024）、临时商品全四元比对防误并、明细落库后即时批量写 source_item 台账；predecessor 链 B 类按点匹配、A 类仅唯一原单挂链；事务设计=generateForCustomer @Transactional、generateForDate 内 TransactionTemplate 逐客户隔离（单客户失败不阻断），SCHEDULED 触发写 t_job_run_log（0 成功/1 失败/2 部分失败，warning_count=失败客户遗漏订单数）；② ScheduledTasks 改双窗口 23:30 预生成次日 + 06:00 兼底当日（均委托 generateForDate(SCHEDULED)，服务级意外失败兜底写失败日志）；③ 接口委托：`/generate/{date}`→generateForDate(MANUAL)、`/generate-by-orders`→generateForOrders（D-025 语义=按勾选定位客户+日期补齐全部遗漏）、新增 `POST /generate/customer/{customerId}/{deliveryDate}`；④ 删除 `generateByDeliveryDate/generateByOrderIds` 及 `selectAggregatedByDeliveryDate/ByOrderIds`（聚合移 Java 侧）；⑤ Mapper 增删：SaleOrderMapper+selectMissedConfirmedOrders（customerId 可空）/selectConfirmedByCustomerAndDate，SaleOrderDetailMapper+selectValidByOrderIdIn（原始行），DeliveryOrderMapper+selectActiveByCustomerAndDate（含 batch_id 空历史单平滑迁移），DeliveryBatchMapper+selectByCustomerAndDateForUpdate，JobRunLog 补状态常量；⑥ 测试：旧 `DeliveryOrderServiceImplTest` 移除生成路径测试保留打印/送达 4 例，新增 `DeliveryGenerationServiceImplTest`×17（幂等/三态/快照/合并/隔离/job 日志/D-025）。⚠️ 待办：编译已知错误 1 处（DeliveryGenerationServiceImpl:367 source_item.createTime 传 Date 应为 LocalDateTime）；`mvn -pl lin-distribution test` 全绿后勾选 §五；验证后 git commit |
| 2026-08-28 | **T5 完成验证收口**：① 旧 `AcceptanceServiceImplTest` 3 例按新口径修正——「录入时实收金额按后端单价重算」超送场景补 `lossReason` 并加断言 reason_type=2（新双向必填拦截）；「损耗为负必须填写原因」断言消息改含「短收差异必须填写原因：白菜」；「提交后状态变更且来源订单进入已验收」补 stub `selectListByAcceptanceId`/`selectListByDeliveryId`（镜像同步入口）并验证历史单跳过 `updateActualBatch`；另修正旧断言 `getLossQuantity`→`getDifferenceQuantity`（s14 已更名，旧测试实际已无法编译）；② 新增 11 例：A类点级展开（跨点合并行按 dept 拆两行、delivered=SUM(allocated)、sku/品名/规格/单位/单价快照自送货行、排序/差异清零、总金额正确）、A类历史单无 source_item 回退按明细行展开（dept 回退单据 delivery_point_id，warn 日志验证）、B/C类 dept 回填（行级 dept 优先/空回退 delivery_point_id）、双向原因四例（短收带原因 reason_type=1/超收缺原因拒绝/短收缺原因拒绝含品名/无差异清空 reason+reason_type）、镜像同步占比分摊（ratio=4/5=0.8，两来源行实收 2.40/1.60、单价锁 source_item 快照 2.50、金额后端重算）、revoke 四例（成功：审计快照 JSON 含验收单号+品名、回 DRAFT+撤销三要素、ACCEPTED→DELIVERED IN 回写、clearActualByOrderId 清镜像 / SETTLED 拒绝含订单号且无快照落库 / 非SUBMITTED 拒绝 / 原因必填且不触发查询）；③ 终态：`mvn -pl lin-distribution test` **102 全绿**（91 旧基线 + 净增 11，AcceptanceServiceImplTest 9→20），全工程 compile 0 error（JDK 17）；④ 勾选 §五 T5 两子项；T5 无前端改动，build:prod 不适用随 T7 补；遗留：`GET /acceptance/by-order/{orderId}`（去验收定位）随 T7 前端第一轮一起做，下一步 T6 退货单 |
| 2026-08-28 | **T6 完成**（退货单，D-032/D-034/Q31）：① 新模块 `ReturnOrderService/Impl` + `ReturnOrderController`（/order/return）+ `ReturnOrderSaveDTO`/`ReturnInspectDTO` + `ReturnOrderStatus` 枚举（0草稿→1已提交(质检中)→2质检完成；**3已完成本期不开放写入口，留结算模块落**，接口面按设计 §6.1 只做 CRUD+submit+inspect）；② 核心口径：数量上限=实收-Σ已退（`sumReturnedByAcceptanceItemIds` 新增 `excludeReturnId` 参数——改草稿重算排除自身占用，已提交/草稿均计入占用、仅 status=3 释放；submit 时二次复核防并发超退）；单价后端锁原验收价（DTO 无价格字段，快照取 accItem.unit_price）；金额后端重算；仅已提交(SUBMITTED)验收单可退（D-032 不撤回历史验收）；明细不属于该验收单拒绝；settle_scope 提交时快照（source_item→来源订单 DISTINCT，任一 SETTLED→1 下期冲销，否则 0 当期冲销；历史单无台账按 0）；inspect 逐行落 quality_result(1可再售/2不可再售)+quality_note，记 inspected_by/time；③ Mapper 调整：ReturnItemMapper 补 `deleteByReturnId`（草稿改单明细整体重建）、sumReturned 加 excludeReturnId；ReturnOrderMapper.xml 删除改**逻辑删除**（is_deleted=1，保留审计；原硬删与 is_deleted 列口径矛盾，顺带修复）；④ 权限对齐 s14 已建菜单：return:list/query/add/edit/remove/submit/inspect（@PreAuthorize 与菜单 perms 一一对应），编号走 BizCodeService("returnOrder","TH",3)（s14 注释：序列首用自建行，无 SQL 变更）；⑤ 测试：新增 `ReturnOrderServiceImplTest`×17（创建锁价重算/超上限拒绝含可退量提示/数量>0/明细归属/仅SUBMITTED可退/改草稿排除自身占用+重建/已提交拒改/提交settle_scope两态/提交复核超退/重复提交/质检成功+结论非法+明细归属+非质检中/删除仅草稿+软删），`mvn -pl lin-distribution test` **119 全绿**（102 基线 + 17 新），全工程 compile 0 error；T6 无前端改动，build:prod 不适用随 T7 补；下一步 T7 前端（第一轮 T2-T4 配套界面） |
| 2026-08-28 | **T7 第一轮完成**（T2-T4 配套界面 + 遗留后端补齐）：① 后端补齐两项——`GET /acceptance/by-order/{orderId}` 去验收定位（`locateBySaleOrder`：source_item 有效分配反查→历史单回退 detail.order_id→过滤 VOIDED→补充单场景优先取已建验收单的最新一张，返回 `AcceptanceByOrderVO`；无验收单带 deliveryId 引导创建草稿）+ `GET /order/delivery/{id}/sources` 来源视图（`selectDeliverySources`：聚合行+source_item 展开的来源订单/配送点/分配量，订单号/点名批量回填防 N+1，历史单 sources 空）；② 订单列表查询修复+增强：`SaleOrderMapper.xml` deliveryOrderCode 子查询原仅走 `detail.order_id`（新模型下永远查不到）→ 改 source_item EXISTS + 历史回退双路，新增 `allocated` 列（口径与 existsValidAllocation 一致）驱动「可撤回」标识与撤回按钮禁用态；③ `sale/index.vue`：方案A残留全部下线（批量验收按钮+确认弹窗+差异阈值 config、撤销验收行按钮，配套 saleDetail.js 三个 actual/* API 封装删除）；新增「去验收」按钮（status=2 专享，调 by-order 定位后跳验收页并带 deliveryId/highlightOrder/create 参数；送货单未送达时给提示）、「可撤回」标识、修改按钮按 §七 矩阵限 status∈{0,1}；生成送货单抽屉文案改统一服务语义；④ `sale/detail.vue`：实收编辑列/防抖自动保存/确认验收/验收差异汇总条全部移除，实收两列改只读镜像展示（列名「差异原因」），新增配送冻结提示条（status≥2），canEditOrder/编辑门禁/草稿路由同步收敛；⑤ `delivery/index.vue` 重写：来源明细 dialog（expand 展示来源订单/配送点/分配量，历史单「—历史数据—」）、作废弹窗（字典原因+other 必填说明+可选「作废后立即重新生成」串联 generateForCustomer）、送达弹窗（PENDING 必选免纸原因字典 D-018/PRINTED 直接确认，改走 `/{id}/delivered`）、补充单 tag、跨点总单标识、作废单状态 tag 悬浮显示作废原因、删除按钮下线（作废替代）、范围筛选（scopeType，D1）；⑥ 测试：`AcceptanceServiceImplTest` 20→26（定位 6 例：命中/无验收单/历史回退/作废排除/未进单/补充单优先）、`DeliveryOrderServiceImplTest` 17→19（来源视图 2 例），`mvn -pl lin-distribution test` **127 全绿**，全工程 compile 0 error，`build:prod` 0 error（仅历史 chunk 体积 warning）；无 SQL 变更（后端仅 Mapper XML 查询增强）；页面级 E2E 冒烟仍待真实环境补验（沿用 T2 备注） |
| 2026-08-28 | **T7 第二轮完成（T7 全部收口）**：① 后端补齐三项——`GET /order/delivery/batch/view?customerId&date` 客户日总表（新 `DeliveryBatchService/Impl` + `DeliveryBatchViewVO`：source_item 按标准品名=coalesce(sku.name,快照品名)×配送点聚合、无价格不拆价 D-027/28；历史单回退送货明细行聚合（dept 回退 delivery_point_id）；服务层品名行聚合+同点防御性合量）+ 权限 `order:delivery:batch`；`GET /workbench/job-alert`（JobRunLogMapper+selectLatestByJobName，最近一次 DELIVERY_GENERATE 非 0 即返回记录驱动告警，Q36/D-037）；验收明细接口增强：`selectItemListByAcceptanceId` 回填 `sources`（按 (delivery_detail_id, customer_dept_id) 匹配 source_item，订单号批量回填防 N+1，AcceptanceItem 增 transient 字段）与 `returnedQuantity`（复用 sumReturnedByAcceptanceItemIds，可退量=实收−累计已退，退货页共用）；② 验收页重写（T5 配套）：**商品分组展示**（组头=品名汇总只读：应送/实收/差异合计；成员=各配送点行可编辑实收，A/B/C 类统一适用，历史单无 dept 自动退化为平铺）；「验收差异」列红负橙正；差异原因列双向（短收/超收 tag+输入，diff≠0 前端预检必填、diff=0 保存时置 null 清空）；**来源对照列**（来源订单号 分配量×单价，历史单"—历史数据—"）；**highlightOrder 定位**（来源含定位订单的行+所在商品组黄色高亮+顶部提示条）；**撤销按钮**（SUBMITTED 专享，原因必填弹窗，perm acceptance:revoke）；订单页「去验收」跳转三态承接（acceptanceId 直接打开 / create=1 自动建草稿并进录入 / 未送达提示）；移动扫码模式保留并适配点级行；③ 退货单页（T6 配套，新 `order/return/index.vue` + `api/order/return.js`）：列表（settle_scope 过滤/状态 tag/质检人）、新建（选已提交验收单→加载明细含累计已退→可退上限=实收−累计已退，改草稿时加回自身占用）、提交（settle_scope 快照提示）、质检（逐行 1可再售/2不可再售+备注，全行必选）、删除（仅草稿）、明细只读查看；⚠️ 质检结论显示不使用 return_quality_result 字典（s14 字典值为 reusable/damaged 字符串，与 DB 整数 1/2 口径不一致，前端本地映射，**字典修正记入债务**）；④ 客户表单补组单策略两字段（docScopeType 下拉 CUSTOMER_DATE/DELIVERY_POINT_DATE + docMergeSameItem 开关，带说明文案）；⑤ 批次总表页（新 `order/batch/view.vue`）：客户+日期查询，标准品名+总量+各点小计（列内 tag+展开行折叠），无价格，含打印（@media print 仅输出总表正文，D-028）；⑥ 「本客户订单已录完·出送货单」按钮（sale/detail.vue 底部，canEditOrder + generateCustomer 权限；未保存/脏数据拦截；确认弹窗提示幂等与作废重建/补充单规则；成功消息含新单号与跳过数）；⑦ workbench（待验收卡指回 /order/acceptance；新增生成异常高优 error 告警条：状态文案+bizDate+遗漏数+message+跳转送货单页）；⑧ OrderAdjustment 写入口隐藏（§5.6 退役节奏第一步）：sale/index.vue 删「调整」按钮/弹窗/方法/import，detail.vue 冻结提示文案改退货单+新增销售订单；⑨ 菜单 SQL `sql/s14d_delivery_ui_menu.sql`（幂等）+ init_all.sql [31] 节：客户日总表 C 菜单（order/batch/view, perms order:delivery:batch）+ 订单调整按钮 status=1 停用（保留只读位）；⑩ 验证：新增测试 6 例（验收明细回填 3 + 批次视图 3），`mvn -pl lin-distribution test` **133 全绿**（127 基线 + 6），全工程 compile 0 error（JDK 17），`build:prod` 0 error（仅历史 chunk 体积 warning）；页面级 E2E 冒烟仍待真实环境补验（沿用 T2/T7 一轮备注）；⚠️ 新债务：return_quality_result 字典值需改为 1/2（或后端改存字典值），下版随 OrderAdjustment 清理一并处理 |
| 2026-08-28 | **T8 完成（文档收口，项目全部收口）**：① 基线《销售订单与送货单关系设计》D-026 修订标注——头部状态升 v1.0r1（修订 2026-08-28）、D-026 决策行改「已修订」并写明「到订单行」作废（实收归验收单、粒度到配送点、订单行层仅留应送分配台账）、§6.2 差异归属图加修订注、§6.3 状态回写表标注已被详细设计 §4.2 替代、§12 增加详细设计/进度文链接并给方案A交互设计文档标注「已随 C1 下线（仅存档）」（顺带修正该节历史相对路径错误 ./DESIGN.md、../03-ui-optimization）；② DESIGN.md——§0 文档体系登记《订单-送货-验收链路详细设计》为专项权威（第4位）；§7.1「此后调整走 order_adjustment」改为护栏口径（拒改/新增销售订单/退货单）；§7.2 送货单补 VOIDED 终态、作废重建/补充单（D-022/023）、免纸送达（D-018）、来源订单精确回写、统一生成服务三入口，并指向详细设计与进度文；§7.3 采购单补「客户日总表」作为采购配货来源之一（D-027/028）；详细设计文档头部状态 🟡设计稿→🟢已定稿；③ manuals——操作手册：迭代总览新增 v1.3 行、目录增 §3.7/§3.8，重写 §3.2（列表列、状态流转护栏、统一生成服务三入口+补单规则+工作台告警）、§3.4（新状态/作废/免纸送达/来源明细/补单规则）、§3.5（独立验收入口、点级分组、双向原因、来源对照、撤销验收、退货关系），新增 §3.7 客户日总表、§3.8 退货单（上限/锁价/质检/settle_scope），第六章 6.10/6.11 重写，Q4 改统一出单语义，编码规则补 HS/YS/TH，术语表补 7 条 v1.3 词条，落款更新；文员快速上手指南：页面地图重排（增验收单/客户日总表/退货单/组单策略，「验收在订单上完成」旧提示纠正）、工作台卡指回验收页+生成异常告警、②增「已录完出单」与改单护栏、③整节重写为验收单页流程（去验收/点级实收/双向原因/来源对照/移动模式/撤销验收/退货）、FAQ 三题重写（送货单开错=作废重建、撤销在验收页、退货单）、取价优先级修正遗留错误（配送点覆盖价已废弃 2026-02）、新客 SOP 补组单策略确认；④ 范围外未动：DEV-ENV-NOTES/DEVELOPMENT.md/打印专项文档；页面级 E2E 冒烟仍待真实环境补验（沿用 T2/T7 备注） |
| 2026-08-28 | **遗留清理（E2E 补验 + 字典债务闭环 + 退役 R2）**：① **s14e 字典口径债务闭环**：新增 `sql/s14e_return_quality_dict_fix.sql`（幂等：reusable/damaged → '1'/'2'，按标签迁移+兑底补齐+自检注释），s14 源文件与 init_all.sql §3.4 同步改为直接插入 '1'/'2'（s14 注释指向 s14e 做存量迁移）；退货单页质检结论改**字典驱动**（`dicts:["return_quality_result"]` + dict-tag 展示 + 下拉 Number(dictValue)，替换 T7 本地映射）；远程库执行前备份 before_s14d_s14e_20260828_120344.sql.gz，s14d+s14e 双跑幂等零报错，验证：客户日总表菜单 2116 就位、字典值 1/2 无残留、订单调整按钮 status=1 停用；② **OrderAdjustment 退役 R2**：`OrderAdjustmentController.add` 标 @Deprecated + javadoc 写明替代路径（新增销售订单/退货单）与 R3 计划，读接口保留（审计回溯）；③ **页面级 E2E 冒烟补验（T2/T7/T8 遗留全部闭环）**：新增 `tests/e2e-smoke.mjs`（playwright-core + 系统 Chrome 无头；登录 → 订单/送货/验收/客户日总表/退货单五页加载 → pageerror/console.error/5xx 集收集 → 关键列抽查）；首跑抓到真问题：`/order/return/page` 404——根因是 **dev.sh 非首次启动跳过 mvn install，spring-boot:run 从 ~/.m2 解析到 8/26 陈旧 lin-distribution jar**（T5-T7 新接口全不在），`./dev.sh build` 刷新后重启，冒烟 **PASS（无 pageerror/console.error/5xx）**，另带鉴权直连抽查 batch-view/job-alert/sources/by-order/return-page 五接口均 200 且数据结构正确；陈旧 jar 坑已记入 DEV-ENV-NOTES §3（后端改码后必须先 build 再 restart，冒烟脚本可快速暴露）；⚠️ 冒烟为只读巡页，不含业务写操作链路（下单/出单/验收全流程写入） |
| 2026-08-28 | **交付 E2E 全链路手工测试脚本**：新增 `tests/E2E全链路测试脚本.md`（T0~T13：环境自检→测试数据准备→录单→出单与幂等→作废重建补单→免纸送达→去验收定位高亮→点级实收+双向原因+镜像→撤销验收→退货单全流程→作废解锁→护栏抽查→客户日总表→清理收尾，每步含操作路径与预期结果，末尾附设计决策对照表 D-0xx/Gxx/C1）；与只读冒烟 `tests/e2e-smoke.mjs` 互补，覆盖此前仅单测验证的写入链路 |
| 2026-08-30 | **送货单客户维度专项（D-038~D-042）落地**：① 新增设计文档《[送货单客户维度设计](送货单客户维度设计.md)》（四层模型 客户策略→批次快照→送货单→来源台账、入口收敛表、三态分支预览口径、客户管理页体现方式、接口契约）；② **后端预览接口**：`DeliveryGeneratePreviewVO` + `DeliveryGenerationService#previewGenerate(deliveryDate, customerId)` + `GET /order/delivery/group-preview`（对齐本文 §6.1 预留契约；customerId 可空=当日全部客户）——与生成同源判定（抽出 `groupRowsByPoint` 供生成/预览共用，复用 `mergeRows`），**只读**：不 UPSERT 批次、不用 `FOR UPDATE`、不落库；额外返策略来源（BATCH_SNAPSHOT/CUSTOMER_CONFIG，D-041）与当日未确认草稿提示（D-042，新增 `SaleOrderMapper#selectDraftOrdersByDate`，避开 `selectSaleOrderList` 不过滤 is_deleted 的坑）；③ **送货单页三步式（D-039）**：新 `order/delivery/generatePreviewList.vue`（客户维度清单纯展示：汇总条 + 作废重建/补充单/草稿警示 + 逐客户行可展开待并入订单与既有单）与 `generatePreviewDrawer.vue`（拉预览 + 确认生成 + 结果回显），`handleGenerate` 不再一句 confirm 直接写库；④ **客户管理页体现（D-040）**：列表新增「送货单组单策略 / 相同商品合并」列 + 按策略筛选（`CustomerMapper.xml#selectCustomerList` 支持 `docScopeType`）、表单补策略生效时机文案、行操作「送货单」→ 新 `partner/customer/customerDeliveryDrawer.vue`（当日生效策略与来源 + 待生成清单 + 当日既有单 + 一键生成/补单，跳送货单页带 `?deliveryDate=`）；⑤ 验证：`DeliveryGenerationServiceImplTest` 新增 6 例预览用例（客户维度清单与生成同口径/空清单+草稿/补充单分支/作废重建覆盖全部已确认订单/批次快照优先/参数校验），该类 28 全绿、`mvn -pl lin-distribution test` 全绿，`vite build` 0 error；手册（操作手册 §2.4/§3.4/§6.10、文员指南）与本文 §6.1/§八 已同步 |
| 2026-09-01 | **P0-A 矩阵总表落地（D-043~D-054 设计定稿 + 首批实现）**：① 新增设计文档《[送货单矩阵总表与批次视图设计](送货单矩阵总表与批次视图设计.md)》（两种输出形态对照、矩阵行/列/格数据模型、列快照与空列规则、价档标记衔接不打价、打印形态与模板绑定加维、打印包、批次两级视图、接口契约、D-043~D-054）；② **矩阵数据模型**：`DeliveryMatrixLayout`（`t_delivery_batch.layout_json` 启用——该列建表至今零写入；列快照 + `colsPerPage=6` + 价档 **append-only**）与 `DeliveryMatrixVO`（行=`t_delivery_order_detail` 行沿用 D-024 不同价必拆行、列=启用点快照含空列、格=`source_item` 按 (detail,dept) 透视、恒等式 `detail.num==Σ格` 自检）；`DeliveryBatchService` 新增 `selectMatrix`/`refreshLayout`，生成成功后定格布局（幂等跳过不刷、内容无变化不刷版本）；③ **顺带修两处既有缺陷**：`selectBatchViewRowsBySource/ByDetail` 行键只到 `sku_id`（同 SKU 不同规格/单位被错并，客户日总表页受影响）→ group by 补规格单位并 join 送货明细；A 类总单模板三级绑定靠 `delivery_point_id = NULL` 恒不成立**巧合**回落全局默认（P1 显式化，本轮先记录）；④ **接口**：`GET /order/delivery/batch/{customerId}/{date}/matrix`（perm `order:delivery:batch`）+ `GET /print/deliveryMatrixData`（票据同口径，固定 `c1..c{colsPerPage}` 槽位保套打列位不漂移，`SecurityConfig` 放行）；⑤ **前端**：`views/order/batch/view.vue` 展开行 → 真矩阵（空列灰底标「当日无单」、adHoc 列标「停」、恒等式不一致行标红+告警条、列块切换、布局版本回显），与「配货总表（不拆价 D-027/28）」双口径一页切换；⑥ **SQL**：`sql/s15_matrix_layout.sql`（幂等：`layout_json` varchar(500)→text，实测 3 点 3 档已 653 字符且 `STRICT_TRANS_TABLES` 下超长会在生成事务内报错）+ init_all.sql [37] 节；远端库执行前备份 `sql/db_bak/before_s15_20260901_000510.sql`，双跑幂等验证通过；⑦ 验证：`DeliveryBatchServiceImplTest` 3→17、`DeliveryGenerationServiceImplTest` 28→29，`mvn -pl lin-distribution test` **277/277**，全工程 `mvn install` 0 error，`vite build` 0 error；远端库事务内合成 A 类跨点数据跑三条矩阵查询后 ROLLBACK（零残留，合并行 5+3=8/2+1=3 恒等式正确）；真实链路（临时实例 8091 + dev 1026，不碰用户 8090/1025）客户 11 大长江 2026-08-28 出 `HS20260901001` 跨点总单 → 矩阵 3 列含 1 空列/`layoutDerived=false` v1/幂等重跑不刷版本/打印数据集 3 实列+3 空槽位；新增 `tests/e2e-delivery-matrix.mjs` 页面 15 项断言全过；⑧ 附带：`vite.config.js` 后端地址改 `VITE_PROXY_TARGET` 可覆盖（默认仍 8090），便于指向临时实例做页面验证；⑨ 遗留：`template_id` 批次快照随 P1 绑定重构一并做；恒等式当前**提示不拦截**待 §十三-1 定稿；P0-B 批次两级视图 / P1 绑定加维 / P2 打印包未开工 |
| 2026-09-01 | **送货单打印两套机制对齐（业务澄清后落地）**：按业务确认重新梳理——**总单（司机/配送员对单，A4 矩阵）** 与 **配送点单（客户签收，针式双列三联带验收栏）** 为两套独立打印机制，均需支持（原设计将二者缠在同一种纸面上导致 D-054「一个批次只一种形态」的误判）。本轮落地【打印/视图层】而非推翻生成核心（业务确认跨点签收模型保留，文员按订单/配送点单维度人工录入实收）：① **总单矩阵：档位标改为备注列**——`DeliveryMatrixLayout` 新增 `tierRemark(rank, tierCount)`（品名保持干净、档位走备注列，`displayProductName` 改为单参返回干净品名）；`DeliveryMatrixVO.RowVO` 新增 `remark` 字段；`selectMatrix` 品名不拼档标、`remark` 输出 `档①/档②`；`PrintController.deliveryMatrixData` 输出 `remark`；`views/order/batch/view.vue` 矩阵表格新增「备注」列显示档位（D-046/D-053 修订：同名不同价拆行、备注列标档位、纸面不打价）；② **配送点单：验收数(留空)列**——`PrintController.deliveryData` 每行新增 `acceptanceNum`（空串，客户签收时送货员手填，纸面空白列，供针式模板绑「验收栏/验收数」）；双列布局（每列 10 行、一页 20 条）与三联为 **JimuReport 模板层**布局（模板内容存 `t_print_template.content`），后端已将字段备齐（菜名/单位/单价/应收数量/验收数；表头客户名+配送点名+日期），模板/前端按需配两套样例模板；③ 边界：本次 **未** 改生成核心 scope_type/跨点合并逻辑，也未做 P0-B 批次两级视图/P1 绑定加维/P2 打印包；④ 验证：`mvn -pl lin-distribution test` **277/277 全绿**（注：需 JDK 17，`JAVA_HOME=/opt/homebrew/opt/openjdk`），`vite build:prod` 0 error；`DeliveryBatchServiceImplTest` 档位断言同步改为「品名干净、档位在 remark」 |

| 2026-09-01 | **P0-B 送货单据页批次两级视图（D-043）落地**：管理单元改为 客户+配送日期（批次），主行=批次聚合、展开子行=该批次各送货单。① **后端 batch-page 聚合**：`DeliveryOrderMapper#selectBatchPage`（按 customer_id+delivery_date 分组，聚合 docCount/printedCount/pendingCount/deliveredCount/voidedCount/supplementCount/pointCount/totalQuantity/totalAmount，子查询 delivery_order_detail 求和）+ `DeliveryBatchPageVO` + `DeliveryOrderService#selectBatchPage`（printForm 取批次布局快照按 scopeType 推导：CUSTOMER_DATE→MATRIX / DELIVERY_POINT_DATE→FLAT；maxReminderLevel 按批内已送达未验收单取最高级，复用接受度制表口径）+ `GET /order/delivery/batch-page`（perm `order:delivery:list`）；② **前端 index.vue 两级视图**：主行「客户/配送日期/形态(总单·矩阵|每点一单)/张数(含补单数)/待打·已打·已送/合计数量/合计金额/待验收(聚红黄)/总表(矩阵批次跳客户日总表页)」，`type=expand` 展开子行（单号/配送点/状态+行内提醒/打印次数/备注 + 明细/来源/打印/送达/作废操作），`@expand-change` 拉子行（复用 listDelivery，按 customerId+deliveryDate），主行按 maxReminderLevel 标色；筛选补齐「客户」下拉（queryParams 增 customerId），范围/状态/配送日期保留；③ 验证：`DeliveryOrderServiceImplTest` 23→25（新增 批次形态推导（跨点归矩阵/每点归平铺）、批内已送达未验收单提醒取最高级），`mvn -pl lin-distribution test` **279/279 全绿**，`vite build:prod` 0 error（3173 模块）；④ 边界：未动生成核心 scope_type；生成入口保持 D-039 三步式；总表打印跳客户日总表矩阵页（复用 `selectMatrix`/`deliveryMatrixData`） |
| 2026-09-01 | **P1 打印绑定加维（D-048）落地**：给 `t_print_template` 加打印形态 `print_form`（MATRIX=跨点总单矩阵 / FLAT=点单平铺），重写绑定解析（按印刷形态 + 绑定层级 客户+配送点>客户>全局默认），后端出候选模板替代前端复刻过滤——修复 A 类总单 delivery_point_id=NULL 靠 `= NULL` 恒不成立、总单模板悄悄回落全局默认的巧合问题。① **DDL**：`sql/s16_print_template_bind_scope.sql`（幂等：t_print_template 加 print_form、t_print_template_version 加 print_form 快照）+ init_all.sql 同步；② **后端**：`PrintTemplate(+Version)` 加 printForm；`selectBindTemplate` 重写（print_form 限定 + bind_type 升序 limit 1）；`selectPrintCandidates`（客户可用已发布模板，同形态排序）；`resolveForDeliveryOrder` 按 scopeType 判形态（CUSTOMER_DATE→MATRIX/其余→FLAT）；`selectPrintCandidates` 返回候选 VO + matchGlobalDefault 标记；`DeliveryPrintCandidateVO`；`GET /order/delivery/{id}/print-candidates`（perm order:delivery:print）；③ **前端**：模板管理表单加「打印形态」下拉+表格列；打印对话框 loadPrintTemplates 改用后端候选（matchGlobalDefault 告警），删前端复刻过滤；④ 验证：`PrintTemplateServiceImplTest` 12→14（候选仅全局默认→命中默认、总单候选含客户级→不命中默认），`mvn -pl lin-distribution test` **281/281** 全绿，`vite build:prod` 0 error；⑤ 边界：`template_id` 批次快照随 P2 打印包一并做；P2 打印包（连续输出多张）未做 |
| 2026-09-01 | **P2 送货单打印包装包（D-050）落地**：`t_print_task` 重建为「送货单打印任务」+ 新增 `t_print_package`（一次打印动作容器，批次级），B 类客户 N 张点单一次输出——建包 → 包内任务清单 → 汇总预览 → 队列连续输出 → 逐张回执。① **DDL**：`sql/s17_delivery_print_package.sql`（t_print_package：batch/customer/date/package_no PKyyyyMMddNNN/media_type/status/total/success/fail/preview_time；t_print_task 重建：package_id/delivery_order_id/seq_no/template_id/copies/device_key/status 0待打1已预览2打印中3成功4失败5取消6跳过/attempt_no/retry_of_task_id/receipt_time/error_msg）+ init_all.sql 同步；② **后端**：`DeliveryPrintPackage(+Task)` 领域、`DeliveryPrintPackageMapper(+TaskMapper)`（展示查询 join 客户/点/模板/送货单）、`DeliveryPrintPackageService(+Impl)`（createPackage 幂等复用/收集批次未作废未送达单/单张无模板任务仍建可后补；previewPackage 标记已预览+留痕；startPackage 包→打印中；receipt 成功才 markPrinted 计次推进 PRINTED、失败不计数记原因、刷包计数；updateTask 逐张改模板份数仅本次生效）、`DeliveryPrintPackageController`（POST 建包/GET {id}/GET list/POST preview/POST start/POST task/{id}/receipt/PUT task/{id}）；③ **前端**：`printPackageDrawer.vue`（抽屉：建包/刷新/状态汇总/包内任务清单可改模板份数/①汇总预览②开始打印前端队列逐张：签发票据→window.open→确认成功失败→回执，失败即停提示剩余未执行）+ index.vue 批次主行新增「打印包」入口；④ 验证：`DeliveryPrintPackageServiceImplTest` 新增 3 例（建包幂等复用/回执成功调 markPrinted/回执失败不调 markPrinted），`mvn -pl lin-distribution test` **284/284** 全绿，`vite build:prod` 0 error（3175 模块）；⑤ 边界：包级数据集 `GET /print/deliveryPackageData`（包级票据）待针式本地助手 W0-4.2/4.3 一并做，主链路用逐单票据+前端队列；`template_id` 批次快照未做；针式实机套打验证受硬件阻塞 |
| 2026-09-02 | **P0-B/P1/P2 真机 E2E 冒烟（页面级，远端库环境）**：新增 `tests/e2e-delivery-batch-p1-p2.mjs`（登录→送货单页批次两级视图→展开子表→打印对话框→打印包抽屉；只读为主，建包仅验证链路无业务副作用）。前置：`./dev.sh build` 刷新兄弟模块（防陈旧 jar 404）→ 后端 8090 + 前端 1025 后台启动（nohup，dev.sh start 前台阻塞会超时被杀）。**DDL 部署（远端库 39.96.76.240，执行前备份 `sql/db_bak/before_s16_s17_20260902_202537.sql`）**：① s16（t_print_template/t_print_template_version 加 print_form，幂等双跑验证）② s17（t_print_package 新建 + t_print_task 重建——旧表 0 行数据，保守重命名 t_print_task_bak_20260902 留审计不物理删）③ 全局默认模板 status 1→2 发布（原为已测试态，selectBind 只认 status=2）。**E2E 抓到并修复 2 个前端真 bug**：① `print-package-drawer` 组件未注册（index.vue `components` 漏 `PrintPackageDrawer`，Vue warn Failed to resolve component）→ 补注册；② 批次主表缺 `row-key`，el-table expand 内容不渲染（listDelivery 已 200 但展开区为空）→ 加 `:row-key`（customerId+deliveryDate 组合）。**验证**：`node tests/e2e-delivery-batch-p1-p2.mjs` **全部通过（无 pageerror/console.error/5xx）**——P0-B 主行聚合列+展开子表+子行操作 14 项、P1 打印对话框（print-candidates 后端已 curl 验证 FLAT 候选+matchGlobalDefault）、P2 抽屉（打开/建包/汇总预览/开始打印/任务清单）；后端 curl 验证：printInfo(999004) 解析全局默认模板、print-candidates 返回候选、建包 PK20260902001 成功（3 任务 seqNo 1-3）。**环境备注**：① dev.sh start 前台阻塞（wait_for_port）导致 shell 超时杀进程组，改 nohup 手动后台启动；② JDK 需 JAVA_HOME=/opt/homebrew/opt/openjdk；③ 真机验证后未做「确认打印/开始打印」写操作（留给真机针式）。此后 `mvn -pl lin-distribution test` 284/284 全绿、`vite build:prod` 0 error 保持 |
| 2026-09-02 | **列表统一按创建/更新时间倒序**：① 送货单 `selectDeliveryOrderList` 补 `order by o.create_time desc, o.id desc`（原无排序按 id 升序）；`selectBatchPage` 批次聚合改 `order by max(o.create_time) desc, max(o.id) desc`（原按 delivery_date desc，改为批次最近单据创建时间倒序，与销售订单/报价单口径一致）；② 销售订单（`selectSaleOrderList` 已是 create_time desc）与报价单（`selectProductSkuQuoteList` 已是 create_time desc）经确认已满足，未改动；③ 验证（真机 8090+1025）：三个列表 curl 均按时间倒序（送货单 HS20260901005→...→08-30；销售订单 XD202609010007→...；报价单 BJ2026082500006→...）；`mvn -pl lin-distribution test` 284/284 全绿；`node tests/e2e-delivery-batch-p1-p2.mjs` 无异常（P1 打印对话框断言改为遍历展开批次找点单子行——跨点总单未配矩阵模板时跳过，属环境数据预期）；④ 环境备注：改动 Mapper XML 后必须 `mvn -pl lin-distribution install -DskipTests` 再重启后端，否则从 ~/.m2 解析陈旧 jar 排序不生效（再次踩坑确认） |
| 2026-09-02 | **通用打印模板落地（s18，两套：点单 FLAT + 总单矩阵 MATRIX）**：① **总单矩阵模板**（司机对单 A4 矩阵，全局默认、所有客户共用）：`sql/s18_universal_print_templates.sql`（幂等，执行前备份 `sql/db_bak/before_s18_20260902_224654.sql`）——`jimu_report` 报表 2599000000000000001（版式：行=品名+单位+备注(档位)、列=配送点 c1~c6 槽位、行尾小计）+ `jimu_report_db` 数据集 hm(单值 head)/dm(列表 rows)（指向 `/print/deliveryMatrixData?deliveryOrderId=${deliveryOrderId}`）+ `t_print_template` id=12「总单矩阵模板（全局默认）」print_form=MATRIX status=2；② **点单模板**沿用现有 id=1（FLAT 全局默认，已验证）；③ **后端补齐**：`PrintController#deliveryMatrixData` head 增加 `c1Name~c6Name` 列名槽位（与 rows c1~c6 对位，模板列头绑定；空列槽位给空串）；④ **验证**：矩阵总单 999007 print-candidates 命中 id=12（MATRIX）/点单 999004 命中 id=1（FLAT）；数据集 head c1Name=[大长江,棠下,华铃,'','','']、rows c2=7.0 与模板 `#{dm.c2}` 对位；两套模板实例均通过 `print-form` 维度解析，与 P1 绑定解析显式化闭环；⑤ 边界：针式双列三联仍属模板层（本地助手 W0-4.2/4.3 到位后配针式版式），当前点单模板为 A4 单列+验收数留空列 |
| 2026-09-03 | **D-055 送货单视图化（大重构，业务定稿 2026-09-03）**：送货单从「独立单证+生成流程」彻底转为**订单的视图**——数据源=订单明细（含变更标记），文员下单后数据即存在，无需生成/定时。① **业务定稿**：总单=客户日总表（矩阵，司机对单 A4 通用模板，标题=<客户>总单）；点单=客户+日期+点（针式双列三联待硬件）；配送前改动=正常更新订单；配送后（已打印）改动=原订单不变、以标记附加明细（加单/换货/退货）；换货=被换行标退货+换入行标换货同组；实收>应收=正常不标记；验收按订单明细对比、只改实收。② **删除**：DeliveryGenerationService/GenerateResultVO/DeliveryGeneratePreviewVO/DeliveryGenerateTrigger/DeliveryByOrdersDTO（生成/补单/作废重建/三态）；4 个生成接口；2 个定时任务（23:30/06:00）+ job_run_log 告警改恒 null；selectMissedConfirmedOrders 等 3 个生成专用查询；**打印包全套**（P2/D-050，含前端抽屉）；**打印拆分配置（W0-2.2）与打印资源登记（W0-6）死代码**（前端 0 调用，286→239 测试）；sale/delivery 前端生成入口/打印包引用/资源管理清理。③ **新增/改造**：`t_sale_order_detail` 加 change_type(0正常/1加单/2换货/3退货)/change_group(换货组)/change_remark；`t_delivery_print_log`（打印分界：客户+日期+点）；`DeliveryPrintLog` Mapper；`DeliveryChangeService(+Impl)` 加单/换货/退货（仅已确认订单）；`DeliveryBatchService#selectPointView`（客户+日期+点 订单明细行含标记，GET /order/delivery/batch/point-view）；`AcceptanceService#createByCustomerPoint`（验收改挂 客户+日期+点 方案乙，应送行=订单明细含标记，加单行实收取 actual_num、退货行应送实收归0，一维一验，POST /acceptance/create-by-point）；`WorkbenchMapper.selectPendingAcceptance` 重写（订单维度：status=1+日期<=当日+无已提交验收+print_time 取打印日志）；`Acceptance(+Item)` 加 deliveryDate/saleOrderDetailId；`DeliveryChangeController` 3 接口。④ **前端**：客户日总表页（/order/batch）成唯一入口——口径三态（矩阵/配货/点单）；点单区=配送点下拉+明细表（订单号/应送/实收/标记tag）+打印/验收入口；订单列表行「配送后变更」按钮+saleChangeDrawer（加单/换货/退货弹窗）；工作台待验收按订单维度；送货单据菜单下线（s21 visible=1，路由保留兼容）。⑤ **展示修复**：矩阵列排除父级单位（parent_id!=0）；当日无单列排后；点单下拉按客户过滤（只显示客户下属配送点）。⑥ **SQL**：s19（标记字段+打印日志表）、s20（验收表：delivery_order_id 改可空+加 delivery_date、acceptance_item 加 sale_order_detail_id、退货表对齐）、s21（菜单下线）——均已执行远端库（备份 before_s19/s20/s21）。⑦ **验证**：`mvn -pl lin-distribution test` **239/239** 全绿（删 32+ 死代码测试、新增 DeliveryChangeServiceImplTest 3 例/AcceptanceServiceImplTest 32 例）；`vite build:prod` 0 error；真机（8090+1025）链路全通过：点单视图（含标记行）、验收创建（YS20260903002 总额76.72 应送=实收）、退货/加单标记写入、工作台待验收（订单维度红2）、矩阵列无父级/无单排后/点单下拉按客户；期间修 4 个真机 bug（验收金额漏算/实收取0镜像默认/加单order_code/acceptance 字段名）。⑧ **边界**：点单针式双列三联模板待硬件（W0-4.2/4.3）；s19-s21 脚本留未提交区（sql 约定不动）；测试临时数据保留可清理。**本地 dev 分支 13 commit 未 push** |
| 2026-09-04 | **D-055 收尾（视图化最后一公里：矩阵/配货改订单明细 + 打印接通 + 脚本补齐 + 死代码清理）**：① **矩阵/配货主口径改订单明细**——`DeliveryBatchMapper` 新增 `selectMatrixDetailsFromOrder`（行=订单明细按五元组合并、应送=SUM(num)、全组为 0 的纯退货行不进矩阵）/`selectMatrixCellsFromOrder`（格=(五元组×配送点) 应送）/`selectBatchViewRowsFromOrder`（配货不拆价），`selectMatrix`/`selectBatchView` 改为**订单明细优先→无数据回退历史送货单口径**（旧 4 个查询标【历史只读】保留）；行身份由 `detailId` 改为**五元组键**（`matrixRowKey`=groupKey+priceKey，历史口径仍用 `d:{detailId}`），`CellRow` 补 skuId/productName/spec/unit/price 五字段与行侧对位；`historyFallback` 改为「走历史口径且无台账」才置 true（旧代码在订单明细口径下永不触发）——**修复 D-055 后新订单在矩阵/配货两口径永远空白的真 bug**（旧口径读 `t_delivery_order_detail`+`t_delivery_source_item`，而这两表 D-055 后不再写入）；② **点单/矩阵/配货读口径统一** `o.status >= 1`（非草稿即应送），新增 `selectValidByCustomerPointDateForView` 供点单展示，`selectValidByCustomerPointDate`（status=1）专供验收创建不动写语义；③ **打印按主体取数**——`/print/deliveryMatrixData`、`/print/deliveryData`、`/print/deliveryHead` 三接口改双主体：`deliveryOrderId`（历史单证）或 `customerId+deliveryDate(+customerDeptId)`（D-055 视图化），参数改 String 容错解析（JimuReport 未替换的 `${param}` 字面量与空串一律视为未传）；票据新增 **bizKey 绑定**（`matrix:{客户}:{日期}` / `point:{客户}:{点}:{日期}`，`PrintTicketPayload` 加 bizKey + `issueByBizKey`/`validateDataAccessByBizKey`，旧三参构造保留）；总单打印标题改 `<客户>总单`、不再按 deliveryId 过滤行；④ **前端接通**——`batch/view.vue` 总单打印改传 customerId+deliveryDate（原来取 `row.deliveryId`，新流程无值即「暂无可用送货单」）、`handlePointPrint` 从「接口正在接入」占位改为真实签发票据+打开 FLAT 模板、配货口径走浏览器打印本页、点单区新增**已打印/未打印** tag（D-055 配送前后分界可见）；⑤ **打印分界登记接通**（新发现缺口）——`DeliveryPrintLogMapper` 原本**无 XML 且零调用方**（`t_delivery_print_log` 永不写入、工作台 print_time 恒 null）→ 补 `DeliveryPrintLogMapper.xml`（insert/countPrinted/selectPrintLogs）+ `DeliveryBatchService#markPrinted/isPrinted` + `POST /order/delivery/print-log`、`GET /order/delivery/print-state`（perm order:delivery:print / :batch）+ 前端打印后登记；⑥ **SQL 脚本补齐（仓库交付风险闭环）**——s15~s21 原本只存在于远端库（仓库与 git 历史零命中，init_all 止于 [34]，新环境按 init_all 重建必缺 D-055 列）→ 按远端实际 schema 反向补齐 `s15_matrix_layout`/`s16_print_template_bind_scope`/`s17（退役占位说明）`/`s18_universal_print_templates`/`s19_delivery_change_marks`/`s20_acceptance_point_dimension`/`s21_delivery_menu_offline`，新增 `s22_print_data_params`（4 个数据集 URL 补 customerId/deliveryDate/customerDeptId/colBlock/ticket 参数 + 参数声明 + 发布全局默认模板）与 `s23_dead_print_objects_retire`（A 段停用 2114 补生成按钮、B 段注释保护删退役表）；`init_all.sql` 追加 [35]~[41] 节并标注 [34] 退役；**本地 MySQL 5.7 空库全链路重建验证**：init_all 0 错误 → 14 项 schema 断言全中（change_* 三列/print_log/acceptance.delivery_date/delivery_order_id 可空/acceptance_item+t_return_item.sale_order_detail_id/print_form×2/layout_json text/菜单 2036 隐藏+2114 停用/退役表已清理）→ s15/s16/s19/s20/s21/s22/s23 各自双跑幂等 0 错误；⑦ **死代码清理**——删 4 个死组件（`order/delivery/generatePreviewDrawer.vue`、`generatePreviewList.vue`、`order/sale/deliveryGenerateDrawer.vue`、`partner/customer/customerDeliveryDrawer.vue`）与 3 处会 404 的入口（录单页「选订单·生成送货单」按钮+抽屉、客户页「送货单」行按钮+抽屉、送货单据页「打印包」列与「作废后重新生成」勾选），`sale/index.vue` 三步抽屉收敛为纯采购链路（删 delivery 分支与 `generateDeliveryByOrders` 调用），`api/order/delivery.js` 删 11 个指向已删接口的死函数（4 generate + 7 print-package）并新增 markDeliveryPrinted/getDeliveryPrintState；后端删 2 个死查询（`selectByCustomerAndDateForUpdate`、`selectActiveByCustomerAndDate`）+ 修 5 处 D-055 陈旧注释/护栏文案（含 `SaleOrderMapper` 里被截断合并的 O_missed 残段 javadoc）；⑧ **验证**：`mvn -pl lin-distribution test` **246/246 全绿**（基线 239：-3 refreshLayout 随定格机制退役、+5 矩阵主口径/历史回退/同名不同价拆行/点单只读口径/配货优先订单明细、+2 票据 bizKey、+3 打印分界登记与判定；`clean` 后计数才准确，陈旧 surefire 报告仍会多捞 2 例）；`vite build:prod` 0 error；真机（8090，远端库）：矩阵打印数据 客户11/2026-08-28（该日**无历史送货单**，旧口径必空）出 3 行×2 点+4 空槽位、identityOk=true、标题「大长江总单」；客户10/2026-09-03 出 4 行（京包菜 4+5=9、淮山薯 2+4=6 跨点合并正确）；点单打印 客户11+棠下 出 1 行含 acceptanceNum 留空列与 changeTag；无票据/错主体/`${param}` 字面量三种异常路径均按预期拒绝或容错；⑨ **本轮未做（已查实的遗留，需业务确认后开工）**——验收提交 `submit` 对点单验收（deliveryOrderId=null）`syncActualMirror` 与状态回写双双空转（实收不回写 `t_sale_order_detail.actual_*`、订单停在 status=1 不进 ACCEPTED，结算以验收单为准故不影响金额）；打印后订单编辑护栏未与打印分界联动（`checkOrderEditable` 仍只看历史 source_item 分配）；客户页「送货单组单策略」两字段与提示语在 D-055 后已无实际作用；**s22/s23 未执行远端库**（按「sql 不动」约定，需备份后人工执行；不执行则新主体打印只能靠页面接口取数、JimuReport 数据集仍按旧 URL 传 deliveryOrderId）；本地 dev 分支新增提交未 push |
| 2026-09-04 | **D-055 收尾二期（下午）：验收回写接通 + 客户组单策略下线 + 浏览器级 E2E**：① **验收提交回写接通**（上午查实的空转缺口，业务已确认要修）——点单验收 `deliveryOrderId=null` 时 `submit` 的实收镜像与状态回写双双空转（实收不回写 `t_sale_order_detail.actual_*`、订单停在 CONFIRMED），现按主体分流：历史送货单口径保持 source_item 占比分摊 + DELIVERED→ACCEPTED；D-055 点单口径新增 `syncActualMirrorFromOrderDetails`（验收行 `sale_order_detail_id` → 1:1 回写 actual_num/actual_price/actual_amount/loss_reason）+ `updateStatusByIds`（新增 Mapper 方法，CONFIRMED→ACCEPTED）；`revoke` 同步分流（点单 ACCEPTED→CONFIRMED 回到未配送可继续改单 + `clearActualByOrderId` 清镜像），并把「来源订单已结算禁止撤销」守卫从只查 source_item 扩到点单口径（新增 `selectByIdIn` 反查来源订单，拒绝提示带单号）——验收后订单被 `checkOrderEditable` 冻结，再退补需先撤销验收或走退货单（与 D-032 一致）；② **打印后编辑护栏按业务定稿保持不拦截**（用户确认「已打印还可以改单」为预期行为，现场补改单优先于留痕），仅以点单区「已打印/未打印」标识与带标记入口引导，设计文 §十九.3 已改写为「已确认的预期行为」；③ **客户组单策略字段下线**（用户确认无用）——`partner/customer/index.vue` 删列表两列（送货单组单策略/相同商品合并）、按策略筛选（queryParams.docScopeType）、表单两项与生效时机提示语；**DB 列与实体字段保留**（历史批次 scope_type/merge_same_item 快照仍可读），彻底清理需单独迁移方案；④ **浏览器级 E2E 冒烟通过**（上午缺登录口令未做，实为 `login.vue` 预置 admin/admin123，此前 curl 失败系命令编码把口令打码）——新增 `logs/agent/e2e-d055-final.mjs`（playwright-core + 系统 Edge 无头，仓库不引入依赖），**17 项断言全过、无 pageerror/console.error/5xx**：矩阵口径 3 行+列头含棠下/华铃、配货口径 3 行、点单明细与「已打印」标识、点单打印开窗 URL 含 ticket/customerId/customerDeptId/deliveryDate、打印后 print-log 200 且标识翻转、总单打印命中矩阵模板 2599…、历史日期（客户11/09-01）仍按订单明细取数（deliveryId 全空）、客户页两列与表单字段确认已下线；⑤ **SQL 级验证**：`selectByIdIn`/`updateStatusByIds`/`updateActualBatch` 三条新 SQL 在远端库事务内跑通后 ROLLBACK（订单 65/66 状态 1→3→回滚回 1、明细 80 实收 4.00/12.00/短收原因→回滚回 0.00，零残留）；⑥ 验证：`mvn -pl lin-distribution clean test` **249/249 全绿**（246 + AcceptanceServiceImplTest 3 例：点单提交回写镜像与状态、点单撤销回退状态并清镜像、点单来源订单已结算禁止撤销），`vite build:prod` 0 error；⑦ 文档：设计文新增 §十九.5（验收回写分流表）/§十九.6（组单策略下线），操作手册升 v1.5（顶部变更横幅 + 迭代总览 + §3.2/3.3/3.4/3.5/3.7 与「生成送货单/补单/打印包/组单策略」各节标注退役、§3.7 重写为三口径唯一入口）、文员指南同步重写（页面地图、每日流程「录完即有数据」、术语表、FAQ）；⑧ 遗留：s22/s23 仍未执行远端（打印数据集参数需执行后报表页才带新参数；s23 B 段 DROP 注释保护）；`t_delivery_print_log` 已有本轮验证产生的 1 条真实登记（客户11/2026-08-28/棠下），可留作演示或清理 |
