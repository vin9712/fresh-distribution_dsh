# Agent 开发任务切片与执行协议（agent-development-brief.md）

> 上游：`DESIGN.md`（权威设计，禁止修改）、`DEVELOPMENT.md`（开发文档）
> 本文件定义 Agent 执行开发任务时的切片规则、验收协议与报告格式。

## 1. 执行协议（必须遵守）

1. **一次只实现一个可独立验证的纵向切片**（对应 DEVELOPMENT.md §6 阶段内的任务）。
2. 动手前先读 `DESIGN.md` 对应章节与 `DEV-ENV-NOTES.md`；改动与设计冲突时**停止并报告用户**，不得反向修改 DESIGN.md。
3. 数据库变更以 `sql/` 目录脚本交付（最新基线 + 增量），脚本必须能在空库执行。
4. 后端接口变更必须同步前端 API 文件与页面，保持一个切片内前后端闭环。
5. **不得声称未执行的验证**：每个切片完成后必须实际运行测试/构建/回归，报告命令、退出状态、未验证项与风险。
6. 前端改动必须通过：生产构建 `npm run build:prod`（exit 0）+ `.dsh-e2e/` 回归（相关页面零 console 错误）。
7. 后端改动必须通过：`mvn test`（相关测试）+ 本地启动冒烟（curl 相关接口）。
8. 编码约束：UTF-8（勿用 PowerShell 5.1 改写源码文件，见 DEV-ENV-NOTES）；金额/数量用 BigDecimal/DECIMAL；时区 Asia/Shanghai。
9. 提交信息格式：`feat(模块): 摘要`，一个切片一次提交，附验证结果。

## 2. 任务切片清单（按 DEVELOPMENT.md §6）

> 商品分类、商品库（SPU）、商品信息（SKU）、客户信息四个模块**原样沿用、不重做**（现有后端接口与已迁移的 Vue3 页面继续使用）；切片只覆盖其上的增量改造与新建模块。

| 切片 | 范围 | 交付物 | 验收 |
|---|---|---|---|
| S0-1 权限与字典 | sys_menu 菜单调整（两级角色）、订单/送货/采购状态字典、单号服务 | SQL 增量 + 后端单号服务 | 单号并发测试 + 菜单权限冒烟 |
| S0-2 表结构基线 | 新建表 + 复用表改造字段的 SQL 基线 | sql/ 基线脚本 | 空库执行通过 |
| S0-3 JimuReport 技术验证 | 集成 JimuReport 社区版：JDK17 兼容、在线打印设计器、数据源、RuoYi JWT 鉴权、多联打印 | 验证报告 + 集成原型 | 设计→渲染→打印全链路演示通过；输出集成方案 |
| S1-1 配送点 | 客户部门→配送点（后端接口/页面/字典） | 前后端闭环 | 页面 CRUD + E2E 冒烟 |
| S1-2 别名与映射 | product_alias、customer_sku_mapping、temp_product | 前后端闭环 | 录单检索联调 |
| S1-3 导入初始化 | SPU 导入（后端 importData/importTemplate + 前端对话框，复用 SKU 模式）；客户报价导入（多客户多行聚合、重复新建、模板=导出模板） | 前后端闭环 | 导入→列表校验、重复跳过提示、操作日志 |
| S2-1 报价有效期 | 客户报价加有效期字段与页面 | SQL+接口+页面 | 有效期筛选测试 |
| S2-2 模板与配送点报价 | 两张新表 + CRUD 页面 | 前后端闭环 | 取价接口测试 |
| S2-3 取价接口 | /price/query 三层优先级 | 接口+单测 | DESIGN 验收 1 自动化测试 |
| S3-1 订单五状态 | t_sale_order 状态改造、确认/撤回/月结接口、字典/页面适配 | SQL+接口+页面 | 状态流转与撤回边界测试 |
| S3-2 工作台 | 待办聚合接口 + 首页工作台 | 前后端闭环 | 待办数据正确 |
| S3-3 订单编辑增强 | 取价接入、快照核对、检索优先级（映射>别名>拼音>品名）、草稿跨天、临时商品转正 | 页面+接口 | 录单 E2E |
| S3-4 加退换 | order_adjustment 表+接口+页面（配送后改订单行数量；SETTLED 禁止） | 前后端闭环 | DESIGN 验收 10 测试 |
| S4-1 采购生成 | 定时任务 + 手动按钮 + 幂等（只含 CONFIRMED） | 后端+单测 | 不重复生成测试 |
| S4-2 采购单页面 | 手工补货、供应商后补、成本、确认/入库 | 前后端闭环 | 状态解耦测试 |
| S5-1 送货生成 | 按客户+配送点+配送日分组、商品合并行、三状态 + 打印计数接口 | 前后端闭环 | 分组/合并正确 |
| S5-2 验收 | acceptance 建单/重算/提交（损耗可负+原因）+ 订单 ACCEPTED | 前后端闭环 | 一单一验、重算测试 |
| S6-1 报表 | 销售日报 + 客户对账单（JimuReport 设计，导出 Excel） | 接口+页面+模板 | 口径核对 |
| S6-2 打印落地 | JimuReport 送货单/验收单/采购单/报价单模板、三级绑定、打印计数、多联 | 模板+集成+页面 | 打印计数/多联/绑定优先级 |
| S6-3 部署 | Nginx/HTTPS/备份/上线检查文档与配置 | 部署文档 | 恢复演练 |

切片可拆小，但每片必须闭环可验证；依赖顺序严格按表从上到下。**S0-3 JimuReport 技术验证必须先行**（打印是 S5 送货模块的关键路径依赖，验证不通过需尽早决策回退 hiprint）。

## 3. 切片完成报告格式（每片一次）

```text
切片：S2-3 取价接口
改动：新增 PriceQueryService/Controller/单测；前端取价调用
验证：
  - mvn -pl lin-admin test -Dtest=PriceQueryServiceTest  → 通过（exit 0）
  - npm run build:prod → exit 0
  - .dsh-e2e 报价页回归 → 零 console 错误
未验证：并发取价压测（无真实流量，人工观察）
风险：有效期边界（当日 00:00 切换）需与用户确认口径
提交：feat(price): 三层取价接口与测试
```

## 4. 环境速查

- 见仓库根目录 `DEV-ENV-NOTES.md`（Node/git/沙箱/PowerShell 5.1 编码陷阱/E2E 工具）。
- 后端本地：profile=local、端口 8090；前端：RuoYi-Vue3 dev 端口 1025。
- E2E：`.dsh-e2e/`（puppeteer-core@8 + 本机 Edge，真实登录 admin/admin123、验证码 1）。
